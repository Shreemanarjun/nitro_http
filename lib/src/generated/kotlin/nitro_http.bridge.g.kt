// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.0
// Generated from: nitro_http.native.dart
package nitro.nitro_http_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.CoroutineStart
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.buffer
import kotlinx.coroutines.runBlocking

// --- Enums ---
@Keep
enum class RawMethod(val nativeValue: Long) {
  GET(0),
  HEAD(1),
  POST(2),
  PUT(3),
  DELETE(4),
  PATCH(5),
  OPTIONS(6),
  TRACE(7),
  CUSTOM(8);

  companion object {
    fun fromNative(v: Long): RawMethod = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawHttpVersionPref(val nativeValue: Long) {
  AUTO(0),
  HTTP11ONLY(1),
  HTTP2(2),
  HTTP2ONLY(3),
  HTTP3(4),
  HTTP3ONLY(5);

  companion object {
    fun fromNative(v: Long): RawHttpVersionPref = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawHttpVersion(val nativeValue: Long) {
  UNKNOWN(0),
  HTTP10(1),
  HTTP11(2),
  HTTP2(3),
  HTTP3(4);

  companion object {
    fun fromNative(v: Long): RawHttpVersion = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawErrorKind(val nativeValue: Long) {
  NONE(0),
  CANCELLED(1),
  TIMEOUTCONNECT(2),
  TIMEOUTREQUEST(3),
  TIMEOUTIDLE(4),
  DNSFAILURE(5),
  CONNECTIONREFUSED(6),
  CONNECTIONRESET(7),
  CONNECTIONFAILED(8),
  TLSHANDSHAKE(9),
  CERTIFICATEINVALID(10),
  CERTIFICATEPINMISMATCH(11),
  CERTIFICATECLIENTAUTH(12),
  TOOMANYREDIRECTS(13),
  PROXYFAILURE(14),
  PROTOCOLERROR(15),
  UNSUPPORTEDSCHEME(16),
  SENDFAILURE(17),
  RECEIVEFAILURE(18),
  DECOMPRESSIONFAILURE(19),
  IO(20),
  CACHEMISS(21),
  ENGINEERROR(22),
  BADREQUEST(23),
  UNKNOWN(24);

  companion object {
    fun fromNative(v: Long): RawErrorKind = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawProxyMode(val nativeValue: Long) {
  SYSTEM(0),
  NONE(1),
  HTTP(2),
  SOCKS5(3),
  SOCKS5HOSTNAME(4);

  companion object {
    fun fromNative(v: Long): RawProxyMode = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawCacheMode(val nativeValue: Long) {
  NORMAL(0),
  NOSTORE(1),
  BYPASS(2),
  ONLYIFCACHED(3),
  REFRESH(4);

  companion object {
    fun fromNative(v: Long): RawCacheMode = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawBodyKind(val nativeValue: Long) {
  NONE(0),
  BYTES(1),
  FILEPATH(2),
  STREAMED(3);

  companion object {
    fun fromNative(v: Long): RawBodyKind = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawChunkKind(val nativeValue: Long) {
  DATA(0),
  DONE(1),
  ERROR(2);

  companion object {
    fun fromNative(v: Long): RawChunkKind = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawEventKind(val nativeValue: Long) {
  DOWNLOADPROGRESS(0),
  UPLOADPROGRESS(1),
  UPLOADDRAIN(2),
  NOTICE(3);

  companion object {
    fun fromNative(v: Long): RawEventKind = values().first { it.nativeValue == v }
  }
}

// --- Structs ---
@androidx.annotation.Keep
data class RawChunk(val bytes: java.nio.ByteBuffer, val requestId: Long, val kind: Long, val aux: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawChunk {
            val bytes = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val requestId = buf.long
            val kind = buf.long
            val aux = buf.long
            return RawChunk(bytes, requestId, kind, aux)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawChunk {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(requestId)
        writeInt(kind)
        writeInt(aux)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(60)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class RawWsFrame(val payload: java.nio.ByteBuffer, val socketId: Long, val opcode: Long, val flags: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawWsFrame {
            val payload = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val socketId = buf.long
            val opcode = buf.long
            val flags = buf.long
            return RawWsFrame(payload, socketId, opcode, flags)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawWsFrame {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(socketId)
        writeInt(opcode)
        writeInt(flags)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(60)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class RawHeader(val name: String, val value: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawHeader {
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val value = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawHeader(name, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawHeader {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawHeader = RawHeader(
                name = map["name"] as String,
                value = map["value"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(72) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(name)
        writeString(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawHeader> support ---
    fun toJson(): Map<String, Any?> = mapOf("name" to name, "value" to value)

}

@androidx.annotation.Keep
data class RawTlsConfig(val verifyCertificates: Boolean, val rootCaSource: Long, val trustedRootsPem: String, val clientCertPem: String, val clientKeyPem: String, val clientKeyPassword: String, val pinnedSpkiSha256: List<String>, val minTlsVersion: Long, val maxTlsVersion: Long, val sniHostname: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawTlsConfig {
            val verifyCertificates = (buf.get().toInt() != 0)
            val rootCaSource = buf.long
            val trustedRootsPem = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val clientCertPem = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val clientKeyPem = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val clientKeyPassword = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val pinnedSpkiSha256 = (0 until buf.int).map { { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }() }
            val minTlsVersion = buf.long
            val maxTlsVersion = buf.long
            val sniHostname = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawTlsConfig(verifyCertificates, rootCaSource, trustedRootsPem, clientCertPem, clientKeyPem, clientKeyPassword, pinnedSpkiSha256, minTlsVersion, maxTlsVersion, sniHostname)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawTlsConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawTlsConfig = RawTlsConfig(
                verifyCertificates = map["verifyCertificates"] as Boolean,
                rootCaSource = (map["rootCaSource"] as Number).toLong(),
                trustedRootsPem = map["trustedRootsPem"] as String,
                clientCertPem = map["clientCertPem"] as String,
                clientKeyPem = map["clientKeyPem"] as String,
                clientKeyPassword = map["clientKeyPassword"] as String,
                pinnedSpkiSha256 = @Suppress("UNCHECKED_CAST") (map["pinnedSpkiSha256"] as List<String>),
                minTlsVersion = (map["minTlsVersion"] as Number).toLong(),
                maxTlsVersion = (map["maxTlsVersion"] as Number).toLong(),
                sniHostname = map["sniHostname"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(241) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(verifyCertificates)
        writeInt(rootCaSource.toLong())
        writeString(trustedRootsPem)
        writeString(clientCertPem)
        writeString(clientKeyPem)
        writeString(clientKeyPassword)
        writeInt32(pinnedSpkiSha256.size)
        pinnedSpkiSha256.forEach { e -> writeString(e) }
        writeInt(minTlsVersion.toLong())
        writeInt(maxTlsVersion.toLong())
        writeString(sniHostname)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawTlsConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("verifyCertificates" to verifyCertificates, "rootCaSource" to rootCaSource, "trustedRootsPem" to trustedRootsPem, "clientCertPem" to clientCertPem, "clientKeyPem" to clientKeyPem, "clientKeyPassword" to clientKeyPassword, "pinnedSpkiSha256" to pinnedSpkiSha256, "minTlsVersion" to minTlsVersion, "maxTlsVersion" to maxTlsVersion, "sniHostname" to sniHostname)

}

@androidx.annotation.Keep
data class RawProxyConfig(val mode: RawProxyMode, val url: String, val username: String, val password: String, val noProxyHosts: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawProxyConfig {
            val mode = RawProxyMode.fromNative(buf.long)
            val url = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val username = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val password = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val noProxyHosts = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawProxyConfig(mode, url, username, password, noProxyHosts)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawProxyConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawProxyConfig = RawProxyConfig(
                mode = RawProxyMode.fromNative(map["mode"] as Long),
                url = map["url"] as String,
                username = map["username"] as String,
                password = map["password"] as String,
                noProxyHosts = map["noProxyHosts"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(152) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(mode.nativeValue)
        writeString(url)
        writeString(username)
        writeString(password)
        writeString(noProxyHosts)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawProxyConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("mode" to mode.nativeValue, "url" to url, "username" to username, "password" to password, "noProxyHosts" to noProxyHosts)

}

@androidx.annotation.Keep
data class RawDnsConfig(val staticOverrides: List<String>, val dohUrl: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawDnsConfig {
            val staticOverrides = (0 until buf.int).map { { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }() }
            val dohUrl = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawDnsConfig(staticOverrides, dohUrl)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawDnsConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawDnsConfig = RawDnsConfig(
                staticOverrides = @Suppress("UNCHECKED_CAST") (map["staticOverrides"] as List<String>),
                dohUrl = map["dohUrl"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(72) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt32(staticOverrides.size)
        staticOverrides.forEach { e -> writeString(e) }
        writeString(dohUrl)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawDnsConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("staticOverrides" to staticOverrides, "dohUrl" to dohUrl)

}

@androidx.annotation.Keep
data class RawCookieConfig(val enabled: Boolean, val persistPath: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawCookieConfig {
            val enabled = (buf.get().toInt() != 0)
            val persistPath = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawCookieConfig(enabled, persistPath)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawCookieConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawCookieConfig = RawCookieConfig(
                enabled = map["enabled"] as Boolean,
                persistPath = map["persistPath"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(37) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(enabled)
        writeString(persistPath)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawCookieConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("enabled" to enabled, "persistPath" to persistPath)

}

@androidx.annotation.Keep
data class RawPoolConfig(val maxConnections: Long, val maxConnectionsPerHost: Long, val idleTimeoutMs: Long, val maxLifetimeMs: Long, val keepAlivePingMs: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawPoolConfig {
            val maxConnections = buf.long
            val maxConnectionsPerHost = buf.long
            val idleTimeoutMs = buf.long
            val maxLifetimeMs = buf.long
            val keepAlivePingMs = buf.long
            return RawPoolConfig(maxConnections, maxConnectionsPerHost, idleTimeoutMs, maxLifetimeMs, keepAlivePingMs)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawPoolConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawPoolConfig = RawPoolConfig(
                maxConnections = (map["maxConnections"] as Number).toLong(),
                maxConnectionsPerHost = (map["maxConnectionsPerHost"] as Number).toLong(),
                idleTimeoutMs = (map["idleTimeoutMs"] as Number).toLong(),
                maxLifetimeMs = (map["maxLifetimeMs"] as Number).toLong(),
                keepAlivePingMs = (map["keepAlivePingMs"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(40) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(maxConnections.toLong())
        writeInt(maxConnectionsPerHost.toLong())
        writeInt(idleTimeoutMs.toLong())
        writeInt(maxLifetimeMs.toLong())
        writeInt(keepAlivePingMs.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawPoolConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("maxConnections" to maxConnections, "maxConnectionsPerHost" to maxConnectionsPerHost, "idleTimeoutMs" to idleTimeoutMs, "maxLifetimeMs" to maxLifetimeMs, "keepAlivePingMs" to keepAlivePingMs)

}

@androidx.annotation.Keep
data class RawClientConfig(val httpVersion: RawHttpVersionPref, val connectTimeoutMs: Long, val requestTimeoutMs: Long, val idleTimeoutMs: Long, val followRedirects: Boolean, val maxRedirects: Long, val enableCompression: Boolean, val enableCache: Boolean, val userAgent: String, val altSvcCachePath: String, val streamChunkBytes: Long, val streamChunkMinContentLength: Long, val streamChunkMaxHoldMs: Long, val defaultHeaders: List<RawHeader>, val tls: RawTlsConfig, val proxy: RawProxyConfig, val dns: RawDnsConfig, val cookies: RawCookieConfig, val pool: RawPoolConfig) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawClientConfig {
            val httpVersion = RawHttpVersionPref.fromNative(buf.long)
            val connectTimeoutMs = buf.long
            val requestTimeoutMs = buf.long
            val idleTimeoutMs = buf.long
            val followRedirects = (buf.get().toInt() != 0)
            val maxRedirects = buf.long
            val enableCompression = (buf.get().toInt() != 0)
            val enableCache = (buf.get().toInt() != 0)
            val userAgent = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val altSvcCachePath = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val streamChunkBytes = buf.long
            val streamChunkMinContentLength = buf.long
            val streamChunkMaxHoldMs = buf.long
            val defaultHeaders = (0 until buf.int).map { RawHeader.decodeFrom(buf) }
            val tls = RawTlsConfig.decodeFrom(buf)
            val proxy = RawProxyConfig.decodeFrom(buf)
            val dns = RawDnsConfig.decodeFrom(buf)
            val cookies = RawCookieConfig.decodeFrom(buf)
            val pool = RawPoolConfig.decodeFrom(buf)
            return RawClientConfig(httpVersion, connectTimeoutMs, requestTimeoutMs, idleTimeoutMs, followRedirects, maxRedirects, enableCompression, enableCache, userAgent, altSvcCachePath, streamChunkBytes, streamChunkMinContentLength, streamChunkMaxHoldMs, defaultHeaders, tls, proxy, dns, cookies, pool)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawClientConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawClientConfig = RawClientConfig(
                httpVersion = RawHttpVersionPref.fromNative(map["httpVersion"] as Long),
                connectTimeoutMs = (map["connectTimeoutMs"] as Number).toLong(),
                requestTimeoutMs = (map["requestTimeoutMs"] as Number).toLong(),
                idleTimeoutMs = (map["idleTimeoutMs"] as Number).toLong(),
                followRedirects = map["followRedirects"] as Boolean,
                maxRedirects = (map["maxRedirects"] as Number).toLong(),
                enableCompression = map["enableCompression"] as Boolean,
                enableCache = map["enableCache"] as Boolean,
                userAgent = map["userAgent"] as String,
                altSvcCachePath = map["altSvcCachePath"] as String,
                streamChunkBytes = (map["streamChunkBytes"] as Number).toLong(),
                streamChunkMinContentLength = (map["streamChunkMinContentLength"] as Number).toLong(),
                streamChunkMaxHoldMs = (map["streamChunkMaxHoldMs"] as Number).toLong(),
                defaultHeaders = @Suppress("UNCHECKED_CAST") (map["defaultHeaders"] as List<RawHeader>),
                tls = RawTlsConfig.fromJson(@Suppress("UNCHECKED_CAST") (map["tls"] as Map<String, Any?>)),
                proxy = RawProxyConfig.fromJson(@Suppress("UNCHECKED_CAST") (map["proxy"] as Map<String, Any?>)),
                dns = RawDnsConfig.fromJson(@Suppress("UNCHECKED_CAST") (map["dns"] as Map<String, Any?>)),
                cookies = RawCookieConfig.fromJson(@Suppress("UNCHECKED_CAST") (map["cookies"] as Map<String, Any?>)),
                pool = RawPoolConfig.fromJson(@Suppress("UNCHECKED_CAST") (map["pool"] as Map<String, Any?>))
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(335) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(httpVersion.nativeValue)
        writeInt(connectTimeoutMs.toLong())
        writeInt(requestTimeoutMs.toLong())
        writeInt(idleTimeoutMs.toLong())
        writeBool(followRedirects)
        writeInt(maxRedirects.toLong())
        writeBool(enableCompression)
        writeBool(enableCache)
        writeString(userAgent)
        writeString(altSvcCachePath)
        writeInt(streamChunkBytes.toLong())
        writeInt(streamChunkMinContentLength.toLong())
        writeInt(streamChunkMaxHoldMs.toLong())
        writeInt32(defaultHeaders.size)
        defaultHeaders.forEach { e -> e.writeFieldsTo(out, buf) }
        tls.writeFieldsTo(out, buf)
        proxy.writeFieldsTo(out, buf)
        dns.writeFieldsTo(out, buf)
        cookies.writeFieldsTo(out, buf)
        pool.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawClientConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("httpVersion" to httpVersion.nativeValue, "connectTimeoutMs" to connectTimeoutMs, "requestTimeoutMs" to requestTimeoutMs, "idleTimeoutMs" to idleTimeoutMs, "followRedirects" to followRedirects, "maxRedirects" to maxRedirects, "enableCompression" to enableCompression, "enableCache" to enableCache, "userAgent" to userAgent, "altSvcCachePath" to altSvcCachePath, "streamChunkBytes" to streamChunkBytes, "streamChunkMinContentLength" to streamChunkMinContentLength, "streamChunkMaxHoldMs" to streamChunkMaxHoldMs, "defaultHeaders" to defaultHeaders, "tls" to tls.toJson(), "proxy" to proxy.toJson(), "dns" to dns.toJson(), "cookies" to cookies.toJson(), "pool" to pool.toJson())

}

@androidx.annotation.Keep
data class RawRequestOptions(val connectTimeoutMs: Long, val requestTimeoutMs: Long, val followRedirects: Long, val maxRedirects: Long, val cacheMode: RawCacheMode, val reportProgress: Boolean, val wantTimings: Boolean, val uploadContentLength: Long, val pinnedSpkiOverride: String, val cancelTokenId: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawRequestOptions {
            val connectTimeoutMs = buf.long
            val requestTimeoutMs = buf.long
            val followRedirects = buf.long
            val maxRedirects = buf.long
            val cacheMode = RawCacheMode.fromNative(buf.long)
            val reportProgress = (buf.get().toInt() != 0)
            val wantTimings = (buf.get().toInt() != 0)
            val uploadContentLength = buf.long
            val pinnedSpkiOverride = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val cancelTokenId = buf.long
            return RawRequestOptions(connectTimeoutMs, requestTimeoutMs, followRedirects, maxRedirects, cacheMode, reportProgress, wantTimings, uploadContentLength, pinnedSpkiOverride, cancelTokenId)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawRequestOptions {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawRequestOptions = RawRequestOptions(
                connectTimeoutMs = (map["connectTimeoutMs"] as Number).toLong(),
                requestTimeoutMs = (map["requestTimeoutMs"] as Number).toLong(),
                followRedirects = (map["followRedirects"] as Number).toLong(),
                maxRedirects = (map["maxRedirects"] as Number).toLong(),
                cacheMode = RawCacheMode.fromNative(map["cacheMode"] as Long),
                reportProgress = map["reportProgress"] as Boolean,
                wantTimings = map["wantTimings"] as Boolean,
                uploadContentLength = (map["uploadContentLength"] as Number).toLong(),
                pinnedSpkiOverride = map["pinnedSpkiOverride"] as String,
                cancelTokenId = (map["cancelTokenId"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(94) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(connectTimeoutMs.toLong())
        writeInt(requestTimeoutMs.toLong())
        writeInt(followRedirects.toLong())
        writeInt(maxRedirects.toLong())
        writeInt(cacheMode.nativeValue)
        writeBool(reportProgress)
        writeBool(wantTimings)
        writeInt(uploadContentLength.toLong())
        writeString(pinnedSpkiOverride)
        writeInt(cancelTokenId.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawRequestOptions> support ---
    fun toJson(): Map<String, Any?> = mapOf("connectTimeoutMs" to connectTimeoutMs, "requestTimeoutMs" to requestTimeoutMs, "followRedirects" to followRedirects, "maxRedirects" to maxRedirects, "cacheMode" to cacheMode.nativeValue, "reportProgress" to reportProgress, "wantTimings" to wantTimings, "uploadContentLength" to uploadContentLength, "pinnedSpkiOverride" to pinnedSpkiOverride, "cancelTokenId" to cancelTokenId)

}

@androidx.annotation.Keep
data class RawRequest(val requestId: Long, val method: RawMethod, val customMethod: String, val url: String, val headers: List<RawHeader>, val bodyKind: RawBodyKind, val bodyFilePath: String, val options: RawRequestOptions) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawRequest {
            val requestId = buf.long
            val method = RawMethod.fromNative(buf.long)
            val customMethod = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val url = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val headers = (0 until buf.int).map { RawHeader.decodeFrom(buf) }
            val bodyKind = RawBodyKind.fromNative(buf.long)
            val bodyFilePath = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val options = RawRequestOptions.decodeFrom(buf)
            return RawRequest(requestId, method, customMethod, url, headers, bodyKind, bodyFilePath, options)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawRequest {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawRequest = RawRequest(
                requestId = (map["requestId"] as Number).toLong(),
                method = RawMethod.fromNative(map["method"] as Long),
                customMethod = map["customMethod"] as String,
                url = map["url"] as String,
                headers = @Suppress("UNCHECKED_CAST") (map["headers"] as List<RawHeader>),
                bodyKind = RawBodyKind.fromNative(map["bodyKind"] as Long),
                bodyFilePath = map["bodyFilePath"] as String,
                options = RawRequestOptions.fromJson(@Suppress("UNCHECKED_CAST") (map["options"] as Map<String, Any?>))
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(200) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(requestId.toLong())
        writeInt(method.nativeValue)
        writeString(customMethod)
        writeString(url)
        writeInt32(headers.size)
        headers.forEach { e -> e.writeFieldsTo(out, buf) }
        writeInt(bodyKind.nativeValue)
        writeString(bodyFilePath)
        options.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawRequest> support ---
    fun toJson(): Map<String, Any?> = mapOf("requestId" to requestId, "method" to method.nativeValue, "customMethod" to customMethod, "url" to url, "headers" to headers, "bodyKind" to bodyKind.nativeValue, "bodyFilePath" to bodyFilePath, "options" to options.toJson())

}

@androidx.annotation.Keep
data class RawTimings(val queueMs: Double, val dnsMs: Double, val connectMs: Double, val tlsMs: Double, val firstByteMs: Double, val redirectMs: Double, val totalMs: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawTimings {
            val queueMs = buf.double
            val dnsMs = buf.double
            val connectMs = buf.double
            val tlsMs = buf.double
            val firstByteMs = buf.double
            val redirectMs = buf.double
            val totalMs = buf.double
            return RawTimings(queueMs, dnsMs, connectMs, tlsMs, firstByteMs, redirectMs, totalMs)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawTimings {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawTimings = RawTimings(
                queueMs = (map["queueMs"] as Number).toDouble(),
                dnsMs = (map["dnsMs"] as Number).toDouble(),
                connectMs = (map["connectMs"] as Number).toDouble(),
                tlsMs = (map["tlsMs"] as Number).toDouble(),
                firstByteMs = (map["firstByteMs"] as Number).toDouble(),
                redirectMs = (map["redirectMs"] as Number).toDouble(),
                totalMs = (map["totalMs"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(56) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeDouble(queueMs)
        writeDouble(dnsMs)
        writeDouble(connectMs)
        writeDouble(tlsMs)
        writeDouble(firstByteMs)
        writeDouble(redirectMs)
        writeDouble(totalMs)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawTimings> support ---
    fun toJson(): Map<String, Any?> = mapOf("queueMs" to queueMs, "dnsMs" to dnsMs, "connectMs" to connectMs, "tlsMs" to tlsMs, "firstByteMs" to firstByteMs, "redirectMs" to redirectMs, "totalMs" to totalMs)

}

@androidx.annotation.Keep
data class RawResponse(val requestId: Long, val errorKind: RawErrorKind, val errorMessage: String, val engineErrorCode: Long, val statusCode: Long, val reasonPhrase: String, val version: RawHttpVersion, val finalUrl: String, val redirectCount: Long, val headers: List<RawHeader>, val body: ByteArray, val fromCache: Boolean, val revalidated: Boolean, val primaryIp: String, val primaryPort: Long, val timings: RawTimings) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawResponse {
            val requestId = buf.long
            val errorKind = RawErrorKind.fromNative(buf.long)
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val engineErrorCode = buf.long
            val statusCode = buf.long
            val reasonPhrase = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val version = RawHttpVersion.fromNative(buf.long)
            val finalUrl = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val redirectCount = buf.long
            val headers = (0 until buf.int).map { RawHeader.decodeFrom(buf) }
            val body = { val _len = buf.int; val _b = ByteArray(_len); buf.get(_b); _b }()
            val fromCache = (buf.get().toInt() != 0)
            val revalidated = (buf.get().toInt() != 0)
            val primaryIp = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val primaryPort = buf.long
            val timings = RawTimings.decodeFrom(buf)
            return RawResponse(requestId, errorKind, errorMessage, engineErrorCode, statusCode, reasonPhrase, version, finalUrl, redirectCount, headers, body, fromCache, revalidated, primaryIp, primaryPort, timings)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawResponse {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawResponse = RawResponse(
                requestId = (map["requestId"] as Number).toLong(),
                errorKind = RawErrorKind.fromNative(map["errorKind"] as Long),
                errorMessage = map["errorMessage"] as String,
                engineErrorCode = (map["engineErrorCode"] as Number).toLong(),
                statusCode = (map["statusCode"] as Number).toLong(),
                reasonPhrase = map["reasonPhrase"] as String,
                version = RawHttpVersion.fromNative(map["version"] as Long),
                finalUrl = map["finalUrl"] as String,
                redirectCount = (map["redirectCount"] as Number).toLong(),
                headers = @Suppress("UNCHECKED_CAST") (map["headers"] as List<RawHeader>),
                body = @Suppress("UNCHECKED_CAST") (map["body"] as ByteArray),
                fromCache = map["fromCache"] as Boolean,
                revalidated = map["revalidated"] as Boolean,
                primaryIp = map["primaryIp"] as String,
                primaryPort = (map["primaryPort"] as Number).toLong(),
                timings = RawTimings.fromJson(@Suppress("UNCHECKED_CAST") (map["timings"] as Map<String, Any?>))
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(338) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(requestId.toLong())
        writeInt(errorKind.nativeValue)
        writeString(errorMessage)
        writeInt(engineErrorCode.toLong())
        writeInt(statusCode.toLong())
        writeString(reasonPhrase)
        writeInt(version.nativeValue)
        writeString(finalUrl)
        writeInt(redirectCount.toLong())
        writeInt32(headers.size)
        headers.forEach { e -> e.writeFieldsTo(out, buf) }
        run { writeInt32(body.size); out.write(body) }
        writeBool(fromCache)
        writeBool(revalidated)
        writeString(primaryIp)
        writeInt(primaryPort.toLong())
        timings.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawResponse> support ---
    fun toJson(): Map<String, Any?> = mapOf("requestId" to requestId, "errorKind" to errorKind.nativeValue, "errorMessage" to errorMessage, "engineErrorCode" to engineErrorCode, "statusCode" to statusCode, "reasonPhrase" to reasonPhrase, "version" to version.nativeValue, "finalUrl" to finalUrl, "redirectCount" to redirectCount, "headers" to headers, "body" to body, "fromCache" to fromCache, "revalidated" to revalidated, "primaryIp" to primaryIp, "primaryPort" to primaryPort, "timings" to timings.toJson())

}

@androidx.annotation.Keep
data class RawResponseHead(val requestId: Long, val errorKind: RawErrorKind, val errorMessage: String, val engineErrorCode: Long, val statusCode: Long, val reasonPhrase: String, val version: RawHttpVersion, val finalUrl: String, val redirectCount: Long, val headers: List<RawHeader>, val fromCache: Boolean, val contentLength: Long, val primaryIp: String, val primaryPort: Long, val timings: RawTimings) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawResponseHead {
            val requestId = buf.long
            val errorKind = RawErrorKind.fromNative(buf.long)
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val engineErrorCode = buf.long
            val statusCode = buf.long
            val reasonPhrase = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val version = RawHttpVersion.fromNative(buf.long)
            val finalUrl = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val redirectCount = buf.long
            val headers = (0 until buf.int).map { RawHeader.decodeFrom(buf) }
            val fromCache = (buf.get().toInt() != 0)
            val contentLength = buf.long
            val primaryIp = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val primaryPort = buf.long
            val timings = RawTimings.decodeFrom(buf)
            return RawResponseHead(requestId, errorKind, errorMessage, engineErrorCode, statusCode, reasonPhrase, version, finalUrl, redirectCount, headers, fromCache, contentLength, primaryIp, primaryPort, timings)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawResponseHead {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawResponseHead = RawResponseHead(
                requestId = (map["requestId"] as Number).toLong(),
                errorKind = RawErrorKind.fromNative(map["errorKind"] as Long),
                errorMessage = map["errorMessage"] as String,
                engineErrorCode = (map["engineErrorCode"] as Number).toLong(),
                statusCode = (map["statusCode"] as Number).toLong(),
                reasonPhrase = map["reasonPhrase"] as String,
                version = RawHttpVersion.fromNative(map["version"] as Long),
                finalUrl = map["finalUrl"] as String,
                redirectCount = (map["redirectCount"] as Number).toLong(),
                headers = @Suppress("UNCHECKED_CAST") (map["headers"] as List<RawHeader>),
                fromCache = map["fromCache"] as Boolean,
                contentLength = (map["contentLength"] as Number).toLong(),
                primaryIp = map["primaryIp"] as String,
                primaryPort = (map["primaryPort"] as Number).toLong(),
                timings = RawTimings.fromJson(@Suppress("UNCHECKED_CAST") (map["timings"] as Map<String, Any?>))
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(277) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(requestId.toLong())
        writeInt(errorKind.nativeValue)
        writeString(errorMessage)
        writeInt(engineErrorCode.toLong())
        writeInt(statusCode.toLong())
        writeString(reasonPhrase)
        writeInt(version.nativeValue)
        writeString(finalUrl)
        writeInt(redirectCount.toLong())
        writeInt32(headers.size)
        headers.forEach { e -> e.writeFieldsTo(out, buf) }
        writeBool(fromCache)
        writeInt(contentLength.toLong())
        writeString(primaryIp)
        writeInt(primaryPort.toLong())
        timings.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawResponseHead> support ---
    fun toJson(): Map<String, Any?> = mapOf("requestId" to requestId, "errorKind" to errorKind.nativeValue, "errorMessage" to errorMessage, "engineErrorCode" to engineErrorCode, "statusCode" to statusCode, "reasonPhrase" to reasonPhrase, "version" to version.nativeValue, "finalUrl" to finalUrl, "redirectCount" to redirectCount, "headers" to headers, "fromCache" to fromCache, "contentLength" to contentLength, "primaryIp" to primaryIp, "primaryPort" to primaryPort, "timings" to timings.toJson())

}

@androidx.annotation.Keep
data class RawEvent(val requestId: Long, val kind: RawEventKind, val a: Long, val b: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawEvent {
            val requestId = buf.long
            val kind = RawEventKind.fromNative(buf.long)
            val a = buf.long
            val b = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawEvent(requestId, kind, a, b, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawEvent {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawEvent = RawEvent(
                requestId = (map["requestId"] as Number).toLong(),
                kind = RawEventKind.fromNative(map["kind"] as Long),
                a = (map["a"] as Number).toLong(),
                b = (map["b"] as Number).toLong(),
                message = map["message"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(68) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(requestId.toLong())
        writeInt(kind.nativeValue)
        writeInt(a.toLong())
        writeInt(b.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawEvent> support ---
    fun toJson(): Map<String, Any?> = mapOf("requestId" to requestId, "kind" to kind.nativeValue, "a" to a, "b" to b, "message" to message)

}

@androidx.annotation.Keep
data class RawCookie(val name: String, val value: String, val domain: String, val path: String, val expiresEpochMs: Long, val secure: Boolean, val httpOnly: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawCookie {
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val value = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val domain = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val path = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val expiresEpochMs = buf.long
            val secure = (buf.get().toInt() != 0)
            val httpOnly = (buf.get().toInt() != 0)
            return RawCookie(name, value, domain, path, expiresEpochMs, secure, httpOnly)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawCookie {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawCookie = RawCookie(
                name = map["name"] as String,
                value = map["value"] as String,
                domain = map["domain"] as String,
                path = map["path"] as String,
                expiresEpochMs = (map["expiresEpochMs"] as Number).toLong(),
                secure = map["secure"] as Boolean,
                httpOnly = map["httpOnly"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(154) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(name)
        writeString(value)
        writeString(domain)
        writeString(path)
        writeInt(expiresEpochMs.toLong())
        writeBool(secure)
        writeBool(httpOnly)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawCookie> support ---
    fun toJson(): Map<String, Any?> = mapOf("name" to name, "value" to value, "domain" to domain, "path" to path, "expiresEpochMs" to expiresEpochMs, "secure" to secure, "httpOnly" to httpOnly)

}

@androidx.annotation.Keep
data class RawCacheConfig(val enabled: Boolean, val directory: String, val maxSizeBytes: Long, val maxEntryBytes: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawCacheConfig {
            val enabled = (buf.get().toInt() != 0)
            val directory = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val maxSizeBytes = buf.long
            val maxEntryBytes = buf.long
            return RawCacheConfig(enabled, directory, maxSizeBytes, maxEntryBytes)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawCacheConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawCacheConfig = RawCacheConfig(
                enabled = map["enabled"] as Boolean,
                directory = map["directory"] as String,
                maxSizeBytes = (map["maxSizeBytes"] as Number).toLong(),
                maxEntryBytes = (map["maxEntryBytes"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(53) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(enabled)
        writeString(directory)
        writeInt(maxSizeBytes.toLong())
        writeInt(maxEntryBytes.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawCacheConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("enabled" to enabled, "directory" to directory, "maxSizeBytes" to maxSizeBytes, "maxEntryBytes" to maxEntryBytes)

}

@androidx.annotation.Keep
data class RawCacheStats(val entryCount: Long, val sizeBytes: Long, val hitCount: Long, val missCount: Long, val revalidationCount: Long, val evictionCount: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawCacheStats {
            val entryCount = buf.long
            val sizeBytes = buf.long
            val hitCount = buf.long
            val missCount = buf.long
            val revalidationCount = buf.long
            val evictionCount = buf.long
            return RawCacheStats(entryCount, sizeBytes, hitCount, missCount, revalidationCount, evictionCount)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawCacheStats {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawCacheStats = RawCacheStats(
                entryCount = (map["entryCount"] as Number).toLong(),
                sizeBytes = (map["sizeBytes"] as Number).toLong(),
                hitCount = (map["hitCount"] as Number).toLong(),
                missCount = (map["missCount"] as Number).toLong(),
                revalidationCount = (map["revalidationCount"] as Number).toLong(),
                evictionCount = (map["evictionCount"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(48) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(entryCount.toLong())
        writeInt(sizeBytes.toLong())
        writeInt(hitCount.toLong())
        writeInt(missCount.toLong())
        writeInt(revalidationCount.toLong())
        writeInt(evictionCount.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawCacheStats> support ---
    fun toJson(): Map<String, Any?> = mapOf("entryCount" to entryCount, "sizeBytes" to sizeBytes, "hitCount" to hitCount, "missCount" to missCount, "revalidationCount" to revalidationCount, "evictionCount" to evictionCount)

}

@androidx.annotation.Keep
data class RawWsConfig(val socketId: Long, val url: String, val protocols: List<String>, val headers: List<RawHeader>, val pingIntervalMs: Long, val maxFrameBytes: Long, val connectTimeoutMs: Long, val tls: RawTlsConfig) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawWsConfig {
            val socketId = buf.long
            val url = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val protocols = (0 until buf.int).map { { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }() }
            val headers = (0 until buf.int).map { RawHeader.decodeFrom(buf) }
            val pingIntervalMs = buf.long
            val maxFrameBytes = buf.long
            val connectTimeoutMs = buf.long
            val tls = RawTlsConfig.decodeFrom(buf)
            return RawWsConfig(socketId, url, protocols, headers, pingIntervalMs, maxFrameBytes, connectTimeoutMs, tls)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawWsConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawWsConfig = RawWsConfig(
                socketId = (map["socketId"] as Number).toLong(),
                url = map["url"] as String,
                protocols = @Suppress("UNCHECKED_CAST") (map["protocols"] as List<String>),
                headers = @Suppress("UNCHECKED_CAST") (map["headers"] as List<RawHeader>),
                pingIntervalMs = (map["pingIntervalMs"] as Number).toLong(),
                maxFrameBytes = (map["maxFrameBytes"] as Number).toLong(),
                connectTimeoutMs = (map["connectTimeoutMs"] as Number).toLong(),
                tls = RawTlsConfig.fromJson(@Suppress("UNCHECKED_CAST") (map["tls"] as Map<String, Any?>))
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(172) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(socketId.toLong())
        writeString(url)
        writeInt32(protocols.size)
        protocols.forEach { e -> writeString(e) }
        writeInt32(headers.size)
        headers.forEach { e -> e.writeFieldsTo(out, buf) }
        writeInt(pingIntervalMs.toLong())
        writeInt(maxFrameBytes.toLong())
        writeInt(connectTimeoutMs.toLong())
        tls.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawWsConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("socketId" to socketId, "url" to url, "protocols" to protocols, "headers" to headers, "pingIntervalMs" to pingIntervalMs, "maxFrameBytes" to maxFrameBytes, "connectTimeoutMs" to connectTimeoutMs, "tls" to tls.toJson())

}

@androidx.annotation.Keep
data class RawWsHandshake(val socketId: Long, val errorKind: RawErrorKind, val errorMessage: String, val engineErrorCode: Long, val statusCode: Long, val negotiatedProtocol: String, val responseHeaders: List<RawHeader>) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawWsHandshake {
            val socketId = buf.long
            val errorKind = RawErrorKind.fromNative(buf.long)
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val engineErrorCode = buf.long
            val statusCode = buf.long
            val negotiatedProtocol = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val responseHeaders = (0 until buf.int).map { RawHeader.decodeFrom(buf) }
            return RawWsHandshake(socketId, errorKind, errorMessage, engineErrorCode, statusCode, negotiatedProtocol, responseHeaders)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawWsHandshake {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawWsHandshake = RawWsHandshake(
                socketId = (map["socketId"] as Number).toLong(),
                errorKind = RawErrorKind.fromNative(map["errorKind"] as Long),
                errorMessage = map["errorMessage"] as String,
                engineErrorCode = (map["engineErrorCode"] as Number).toLong(),
                statusCode = (map["statusCode"] as Number).toLong(),
                negotiatedProtocol = map["negotiatedProtocol"] as String,
                responseHeaders = @Suppress("UNCHECKED_CAST") (map["responseHeaders"] as List<RawHeader>)
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(140) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(socketId.toLong())
        writeInt(errorKind.nativeValue)
        writeString(errorMessage)
        writeInt(engineErrorCode.toLong())
        writeInt(statusCode.toLong())
        writeString(negotiatedProtocol)
        writeInt32(responseHeaders.size)
        responseHeaders.forEach { e -> e.writeFieldsTo(out, buf) }
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawWsHandshake> support ---
    fun toJson(): Map<String, Any?> = mapOf("socketId" to socketId, "errorKind" to errorKind.nativeValue, "errorMessage" to errorMessage, "engineErrorCode" to engineErrorCode, "statusCode" to statusCode, "negotiatedProtocol" to negotiatedProtocol, "responseHeaders" to responseHeaders)

}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableInt = NitroNullableInt(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableInt> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableDouble = NitroNullableDouble(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableDouble> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableBool = NitroNullableBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptInt64(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptInt64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroOptInt64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptInt64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptInt64 = NitroOptInt64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptInt64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptFloat64(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptFloat64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroOptFloat64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptFloat64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptFloat64 = NitroOptFloat64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptFloat64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroOptBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptBool = NitroOptBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

/**
 * Contract for the [NitroHttpNative] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroHttpNativeSpec {
    val applicationContext: Context get() = NitroHttpNativeJniBridge.applicationContext
    val activity: Activity? get() = NitroHttpNativeJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_http.native.dart:739
    fun engineVersion(): String
    // source: nitro_http.native.dart:741
    fun supportsHttp3(): Boolean
    // source: nitro_http.native.dart:742
    fun supportsWebSockets(): Boolean
    // source: nitro_http.native.dart:743
    fun supportsBrotli(): Boolean
    // source: nitro_http.native.dart:744
    fun supportsZstd(): Boolean
    // source: nitro_http.native.dart:748
    fun resetNative(): Unit
    // source: nitro_http.native.dart:754
    fun configureClient(config: RawClientConfig): Unit
    // source: nitro_http.native.dart:757
    suspend fun sendBuffered(request: RawRequest, body: java.nio.ByteBuffer): RawResponse
    // source: nitro_http.native.dart:780
    fun sendBufferedCoalesced(callId: Long, request: RawRequest, body: java.nio.ByteBuffer, dartPort: Long): Unit
    // source: nitro_http.native.dart:797
    fun releaseRecord(address: Long): Unit
    // source: nitro_http.native.dart:800
    suspend fun startStreamed(request: RawRequest, body: java.nio.ByteBuffer): RawResponseHead
    // source: nitro_http.native.dart:805
    fun cancel(requestId: Long): Unit
    // source: nitro_http.native.dart:806
    fun cancelAll(): Unit
    // source: nitro_http.native.dart:818
    fun cancelToken(tokenId: Long, reason: String): Unit
    // source: nitro_http.native.dart:826
    fun releaseCancelToken(tokenId: Long): Unit
    // source: nitro_http.native.dart:841
    fun grantCredit(requestId: Long, chunkCount: Long, ackedChunks: Long): Unit
    // source: nitro_http.native.dart:845
    fun feedUploadChunk(requestId: Long, chunk: java.nio.ByteBuffer): Long
    // source: nitro_http.native.dart:847
    fun finishUpload(requestId: Long): Unit
    // source: nitro_http.native.dart:850
    fun failUpload(requestId: Long, message: String): Unit
    // source: nitro_http.native.dart:853
    fun getCookies(url: String): List<RawCookie>
    // source: nitro_http.native.dart:854
    fun setCookie(cookie: RawCookie): Unit
    // source: nitro_http.native.dart:855
    fun clearCookies(): Unit
    // source: nitro_http.native.dart:856
    fun flushCookies(): Unit
    // source: nitro_http.native.dart:860
    fun configureCache(config: RawCacheConfig): Unit
    // source: nitro_http.native.dart:865
    suspend fun prefetch(request: RawRequest): RawResponse
    // source: nitro_http.native.dart:867
    fun clearCache(): Unit
    // source: nitro_http.native.dart:868
    fun cacheStats(): RawCacheStats
    // source: nitro_http.native.dart:873
    suspend fun wsConnect(config: RawWsConfig): RawWsHandshake
    // source: nitro_http.native.dart:876
    fun wsSend(opcode: Long, payload: java.nio.ByteBuffer): Long
    // source: nitro_http.native.dart:878
    fun wsClose(code: Long, reason: String): Unit
    // source: nitro_http.native.dart:881
    fun wsGrantCredit(frameCount: Long, ackedFrames: Long): Unit
    val chunks: Flow<RawChunk>
    val events: Flow<RawEvent>
    val wsFrames: Flow<RawWsFrame>
}

@Keep
object NitroHttpNativeJniBridge {
    private val _implementations = java.util.concurrent.ConcurrentHashMap<Long, HybridNitroHttpNativeSpec>()
    private val _idCounter = java.util.concurrent.atomic.AtomicLong(0)
    private var _factory: (() -> HybridNitroHttpNativeSpec)? = null
    private val _asyncExecutor = java.util.concurrent.Executors.newCachedThreadPool()

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    private val _zeroCopyPool = java.util.concurrent.ConcurrentHashMap<Int, java.util.concurrent.ArrayBlockingQueue<java.nio.ByteBuffer>>()
    private const val _kZeroCopyPoolPerSize = 8

    /// Borrows a direct buffer of exactly [size] bytes, reusing a released one
    /// when available. Use this instead of allocateDirect() for @zeroCopy
    /// returns. Bounded and non-blocking — it never makes a caller wait.
    @JvmStatic fun acquireZeroCopyBuffer(size: Int): java.nio.ByteBuffer {
        val reused = _zeroCopyPool[size]?.poll()
        if (reused != null) { reused.clear(); return reused }
        return java.nio.ByteBuffer.allocateDirect(size).order(java.nio.ByteOrder.nativeOrder())
    }

    /// Called from the native release path once Dart has released the view.
    /// offer() drops the buffer when the pool is full, so memory stays bounded.
    @JvmStatic fun releaseZeroCopyBuffer(buf: java.nio.ByteBuffer) {
        if (!buf.isDirect) return
        _zeroCopyPool.computeIfAbsent(buf.capacity()) {
            java.util.concurrent.ArrayBlockingQueue(_kZeroCopyPoolPerSize)
        }.offer(buf)
    }

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun registerFactory(factory: () -> HybridNitroHttpNativeSpec, context: Context) {
        applicationContext = context
        _factory = factory
        if (_implementations.isEmpty()) { initialize(this::class.java) }
    }

    @JvmStatic
    fun create_instance_call(key: String): Long {
        val factory = _factory ?: throw IllegalStateException(
            "NitroHttpNative: no factory registered. Call registerFactory() in onAttachedToEngine().")
        val id = _idCounter.getAndIncrement()
        val impl = factory()
        _implementations[id] = impl
        impl.onAttached()
        return id
    }

    @JvmStatic
    fun destroy_instance_call(instanceId: Long) {
        _implementations.remove(instanceId)?.onDetached()
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        _implementations.values.forEach { it.onActivityAttached(newActivity) }
    }

    fun onActivityDetached() {
        activity = null
        _implementations.values.forEach { it.onActivityDetached() }
    }

    // @NitroNativeAsync helpers — post primitive results via Dart_PostCObject_DL.
    @JvmStatic external fun postNullToPort(dartPort: Long)
    @JvmStatic external fun postInt64ToPort(dartPort: Long, value: Long)
    @JvmStatic external fun postDoubleToPort(dartPort: Long, value: Double)
    @JvmStatic external fun postBoolToPort(dartPort: Long, value: Boolean)
    @JvmStatic external fun postStringToPort(dartPort: Long, value: String)
    // Nullable prim helpers: malloc NitroOptXxx on native heap, post address as kInt64.
    // Dart decodes via Pointer<NitroOptXxx>.fromAddress and frees via the <lib>_nitro_free export.
    @JvmStatic external fun postOptInt64ToPort(dartPort: Long, value: Long, hasValue: Boolean)
    @JvmStatic external fun postOptFloat64ToPort(dartPort: Long, value: Double, hasValue: Boolean)
    @JvmStatic external fun postOptBoolToPort(dartPort: Long, value: Boolean, hasValue: Boolean)
    // Record/list-record helper: malloc a [4B len][payload] native buffer, copy
    // value into it, post the address as kInt64. Dart decodes via Pointer<Uint8>.
    // null posts address 0 (nullptr) — NOT Dart_CObject_kNull — matching how
    // nullable record returns are decoded elsewhere (a pointer-typed kInt64,
    // never a bare kNull CObject).
    @JvmStatic external fun postBytesToPort(dartPort: Long, value: ByteArray?)
    // Writes a thrown exception's name/message into the fresh-per-call
    // NitroError* whose address Dart passed as errPtr, before the trampoline
    // still posts exactly one message so the port fires either way.
    @JvmStatic external fun reportNativeAsyncError(errPtr: Long, name: String, message: String)

    // source: nitro_http.native.dart:739
    @JvmStatic fun engineVersion_call(instanceId: Long): String {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        return impl.engineVersion()
    }
    // source: nitro_http.native.dart:741
    @JvmStatic fun supportsHttp3_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        return impl.supportsHttp3()
    }
    // source: nitro_http.native.dart:742
    @JvmStatic fun supportsWebSockets_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        return impl.supportsWebSockets()
    }
    // source: nitro_http.native.dart:743
    @JvmStatic fun supportsBrotli_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        return impl.supportsBrotli()
    }
    // source: nitro_http.native.dart:744
    @JvmStatic fun supportsZstd_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        return impl.supportsZstd()
    }
    // source: nitro_http.native.dart:748
    @JvmStatic fun resetNative_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.resetNative()
    }
    // source: nitro_http.native.dart:754
    @JvmStatic fun configureClient_call(instanceId: Long, config: ByteArray): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        val configBuf = java.nio.ByteBuffer.wrap(config).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        configBuf.getInt() // skip Dart 4-byte outer length prefix
        val configDecoded = RawClientConfig.decodeFrom(configBuf)
        impl.configureClient(configDecoded)
    }
    // source: nitro_http.native.dart:757
    @JvmStatic fun sendBuffered_call(instanceId: Long, request: ByteArray, body: java.nio.ByteBuffer, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val requestBuf = java.nio.ByteBuffer.wrap(request).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        requestBuf.getInt() // skip Dart 4-byte outer length prefix
        val requestDecoded = RawRequest.decodeFrom(requestBuf)
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.sendBuffered(requestDecoded, body) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_http.native.dart:780
    @JvmStatic fun sendBufferedCoalesced_call(instanceId: Long, callId: Long, request: ByteArray, body: java.nio.ByteBuffer, dartPort: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        val requestBuf = java.nio.ByteBuffer.wrap(request).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        requestBuf.getInt() // skip Dart 4-byte outer length prefix
        val requestDecoded = RawRequest.decodeFrom(requestBuf)
        impl.sendBufferedCoalesced(callId, requestDecoded, body, dartPort)
    }
    // source: nitro_http.native.dart:797
    @JvmStatic fun releaseRecord_call(instanceId: Long, address: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.releaseRecord(address)
    }
    // source: nitro_http.native.dart:800
    @JvmStatic fun startStreamed_call(instanceId: Long, request: ByteArray, body: java.nio.ByteBuffer, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val requestBuf = java.nio.ByteBuffer.wrap(request).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        requestBuf.getInt() // skip Dart 4-byte outer length prefix
        val requestDecoded = RawRequest.decodeFrom(requestBuf)
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.startStreamed(requestDecoded, body) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_http.native.dart:805
    @JvmStatic fun cancel_call(instanceId: Long, requestId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.cancel(requestId)
    }
    // source: nitro_http.native.dart:806
    @JvmStatic fun cancelAll_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.cancelAll()
    }
    // source: nitro_http.native.dart:818
    @JvmStatic fun cancelToken_call(instanceId: Long, tokenId: Long, reason: String): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.cancelToken(tokenId, reason)
    }
    // source: nitro_http.native.dart:826
    @JvmStatic fun releaseCancelToken_call(instanceId: Long, tokenId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.releaseCancelToken(tokenId)
    }
    // source: nitro_http.native.dart:841
    @JvmStatic fun grantCredit_call(instanceId: Long, requestId: Long, chunkCount: Long, ackedChunks: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.grantCredit(requestId, chunkCount, ackedChunks)
    }
    // source: nitro_http.native.dart:845
    @JvmStatic fun feedUploadChunk_call(instanceId: Long, requestId: Long, chunk: java.nio.ByteBuffer): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        return impl.feedUploadChunk(requestId, chunk)
    }
    // source: nitro_http.native.dart:847
    @JvmStatic fun finishUpload_call(instanceId: Long, requestId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.finishUpload(requestId)
    }
    // source: nitro_http.native.dart:850
    @JvmStatic fun failUpload_call(instanceId: Long, requestId: Long, message: String): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.failUpload(requestId, message)
    }
    // source: nitro_http.native.dart:853
    @JvmStatic fun getCookies_call(instanceId: Long, url: String): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        val result = impl.getCookies(url)
        val itemBufs = ArrayList<ByteArray>(result.size)
        val tmpBuf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        for (item in result) {
            val tmpOut = java.io.ByteArrayOutputStream(154)
            item.writeFieldsTo(tmpOut, tmpBuf)
            itemBufs.add(tmpOut.toByteArray())
        }
        // payload = [4B count][8B × n offsets][item bytes...]
        var offsetPos = 4 + 8L * result.size  // start of item data in payload
        val offsets = LongArray(result.size)
        for (i in result.indices) { offsets[i] = offsetPos; offsetPos += itemBufs[i].size }
        val payloadBuf = java.nio.ByteBuffer.allocate(offsetPos.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        payloadBuf.putInt(result.size)
        offsets.forEach { payloadBuf.putLong(it) }
        itemBufs.forEach { payloadBuf.put(it) }
        val payload = payloadBuf.array()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
    // source: nitro_http.native.dart:854
    @JvmStatic fun setCookie_call(instanceId: Long, cookie: ByteArray): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        val cookieBuf = java.nio.ByteBuffer.wrap(cookie).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        cookieBuf.getInt() // skip Dart 4-byte outer length prefix
        val cookieDecoded = RawCookie.decodeFrom(cookieBuf)
        impl.setCookie(cookieDecoded)
    }
    // source: nitro_http.native.dart:855
    @JvmStatic fun clearCookies_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.clearCookies()
    }
    // source: nitro_http.native.dart:856
    @JvmStatic fun flushCookies_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.flushCookies()
    }
    // source: nitro_http.native.dart:860
    @JvmStatic fun configureCache_call(instanceId: Long, config: ByteArray): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        val configBuf = java.nio.ByteBuffer.wrap(config).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        configBuf.getInt() // skip Dart 4-byte outer length prefix
        val configDecoded = RawCacheConfig.decodeFrom(configBuf)
        impl.configureCache(configDecoded)
    }
    // source: nitro_http.native.dart:865
    @JvmStatic fun prefetch_call(instanceId: Long, request: ByteArray, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val requestBuf = java.nio.ByteBuffer.wrap(request).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        requestBuf.getInt() // skip Dart 4-byte outer length prefix
        val requestDecoded = RawRequest.decodeFrom(requestBuf)
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.prefetch(requestDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_http.native.dart:867
    @JvmStatic fun clearCache_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.clearCache()
    }
    // source: nitro_http.native.dart:868
    @JvmStatic fun cacheStats_call(instanceId: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        val result = impl.cacheStats()
        return result.encode()
    }
    // source: nitro_http.native.dart:873
    @JvmStatic fun wsConnect_call(instanceId: Long, config: ByteArray, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val configBuf = java.nio.ByteBuffer.wrap(config).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        configBuf.getInt() // skip Dart 4-byte outer length prefix
        val configDecoded = RawWsConfig.decodeFrom(configBuf)
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.wsConnect(configDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_http.native.dart:876
    @JvmStatic fun wsSend_call(instanceId: Long, opcode: Long, payload: java.nio.ByteBuffer): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        return impl.wsSend(opcode, payload)
    }
    // source: nitro_http.native.dart:878
    @JvmStatic fun wsClose_call(instanceId: Long, code: Long, reason: String): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.wsClose(code, reason)
    }
    // source: nitro_http.native.dart:881
    @JvmStatic fun wsGrantCredit_call(instanceId: Long, frameCount: Long, ackedFrames: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroHttpNative instance $instanceId not registered")
        impl.wsGrantCredit(frameCount, ackedFrames)
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_chunks(dartPort: Long, item: RawChunk): Boolean

    @JvmStatic fun nitro_http_register_chunks_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("chunks", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.chunks
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_chunks(dartPort, item)) {
                        _streamJobs.remove(Pair("chunks", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_http_release_chunks_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("chunks", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_events(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_http_register_events_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("events", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.events
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_events(dartPort, item.encode())) {
                        _streamJobs.remove(Pair("events", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_http_release_events_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("events", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_wsFrames(dartPort: Long, item: RawWsFrame): Boolean

    @JvmStatic fun nitro_http_register_ws_frames_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("wsFrames", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.wsFrames
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_wsFrames(dartPort, item)) {
                        _streamJobs.remove(Pair("wsFrames", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_http_release_ws_frames_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("wsFrames", dartPort))?.cancel()
    }
}
