// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.4
// Generated from: nitro_http.native.dart
// ignore_for_file: no_leading_underscores_for_local_identifiers, prefer_typing_uninitialized_variables, non_constant_identifier_names, unused_element, unused_field, unused_import
/// Native (dart:ffi) implementation of [NitroHttpNative]. Web builds never
/// compile this library — the platform shim resolves to the web bridge.
library;

import 'package:nitro/nitro.dart';

import '../../nitro_http.native.dart';

// --- Struct FFI representations (generated by Nitrogen) ---

final class RawChunkFfi extends Struct {
  external Pointer<Uint8> bytes;
  @Int64()
  external int bytesLength;
  @Int64()
  external int requestId;
  @Int64()
  external int kind;
  @Int64()
  external int aux;
}

extension RawChunkFfiExt on RawChunkFfi {
  RawChunk toDart() {
    return RawChunk(
      bytes: Uint8List.fromList(bytes.asTypedList(bytesLength)),
      requestId: requestId,
      kind: kind,
      aux: aux,
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  ///
  /// [nativeFree] must be the free function matching the allocator that
  /// produced the fields — for native-returned structs, the module's
  /// `_nitroFree` (the exported C-runtime free). package:ffi's malloc.free
  /// is CoTaskMemFree on Windows and corrupts the heap on these pointers.
  void freeFields(void Function(Pointer<NativeType>) nativeFree) {}
}

extension RawChunkExt on RawChunk {
  Pointer<RawChunkFfi> toNative(Arena arena) {
    final ptr = arena<RawChunkFfi>();
    ptr.ref.bytes = bytes.toPointer(arena);
    ptr.ref.bytesLength = bytes.length;
    ptr.ref.requestId = requestId;
    ptr.ref.kind = kind;
    ptr.ref.aux = aux;
    return ptr;
  }
}

final class RawWsFrameFfi extends Struct {
  external Pointer<Uint8> payload;
  @Int64()
  external int payloadLength;
  @Int64()
  external int socketId;
  @Int64()
  external int opcode;
  @Int64()
  external int flags;
}

extension RawWsFrameFfiExt on RawWsFrameFfi {
  RawWsFrame toDart() {
    return RawWsFrame(
      payload: Uint8List.fromList(payload.asTypedList(payloadLength)),
      socketId: socketId,
      opcode: opcode,
      flags: flags,
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  ///
  /// [nativeFree] must be the free function matching the allocator that
  /// produced the fields — for native-returned structs, the module's
  /// `_nitroFree` (the exported C-runtime free). package:ffi's malloc.free
  /// is CoTaskMemFree on Windows and corrupts the heap on these pointers.
  void freeFields(void Function(Pointer<NativeType>) nativeFree) {}
}

extension RawWsFrameExt on RawWsFrame {
  Pointer<RawWsFrameFfi> toNative(Arena arena) {
    final ptr = arena<RawWsFrameFfi>();
    ptr.ref.payload = payload.toPointer(arena);
    ptr.ref.payloadLength = payload.length;
    ptr.ref.socketId = socketId;
    ptr.ref.opcode = opcode;
    ptr.ref.flags = flags;
    return ptr;
  }
}

// --- Struct Native Proxies (zero-copy, lazy field access) ---

/// Zero-copy proxy for [RawChunk].
/// Extends [RawChunk] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `RawChunkProxy <: RawChunk`, a `Stream<RawChunkProxy>`
/// satisfies `Stream<RawChunk>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_http_release_RawChunk'.
/// Ownership: this proxy owns the generated struct shell. Zero-copy
/// field buffers remain owned by the native implementation and must
/// stay valid until this proxy is released by the finalizer or
/// [toDartAndRelease].
final class RawChunkProxy extends RawChunk implements Finalizable {
  final Pointer<RawChunkFfi> _native;

  static NativeFinalizer? _finalizer;
  static void Function(Pointer<Void>)? _releaseFn;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
        'nitro_http_release_RawChunk',
      ),
    );
    _releaseFn ??= dylib
        .lookupFunction<
          Void Function(Pointer<Void>),
          void Function(Pointer<Void>)
        >('nitro_http_release_RawChunk');
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] on the struct shell after passing it here,
  /// and do not free zero-copy field buffers while the proxy may be read.
  RawChunkProxy(this._native)
    : super(bytes: Uint8List(0), requestId: 0, kind: 0, aux: 0) {
    assert(
      _finalizer != null,
      'RawChunkProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.',
    );
    _finalizer!.attach(
      this,
      _native.cast(),
      detach: this,
      externalSize: sizeOf<RawChunkFfi>(),
    );
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  Uint8List get bytes => _native.ref.bytes.asTypedList(_native.ref.bytesLength);
  @override
  int get requestId => _native.ref.requestId;
  @override
  int get kind => _native.ref.kind;
  @override
  int get aux => _native.ref.aux;

  /// Eagerly copies all fields to a plain [RawChunk] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  RawChunk toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    _releaseFn!(_native.cast());
    return v;
  }
}

/// Zero-copy proxy for [RawWsFrame].
/// Extends [RawWsFrame] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `RawWsFrameProxy <: RawWsFrame`, a `Stream<RawWsFrameProxy>`
/// satisfies `Stream<RawWsFrame>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_http_release_RawWsFrame'.
/// Ownership: this proxy owns the generated struct shell. Zero-copy
/// field buffers remain owned by the native implementation and must
/// stay valid until this proxy is released by the finalizer or
/// [toDartAndRelease].
final class RawWsFrameProxy extends RawWsFrame implements Finalizable {
  final Pointer<RawWsFrameFfi> _native;

  static NativeFinalizer? _finalizer;
  static void Function(Pointer<Void>)? _releaseFn;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
        'nitro_http_release_RawWsFrame',
      ),
    );
    _releaseFn ??= dylib
        .lookupFunction<
          Void Function(Pointer<Void>),
          void Function(Pointer<Void>)
        >('nitro_http_release_RawWsFrame');
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] on the struct shell after passing it here,
  /// and do not free zero-copy field buffers while the proxy may be read.
  RawWsFrameProxy(this._native)
    : super(payload: Uint8List(0), socketId: 0, opcode: 0, flags: 0) {
    assert(
      _finalizer != null,
      'RawWsFrameProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.',
    );
    _finalizer!.attach(
      this,
      _native.cast(),
      detach: this,
      externalSize: sizeOf<RawWsFrameFfi>(),
    );
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  Uint8List get payload =>
      _native.ref.payload.asTypedList(_native.ref.payloadLength);
  @override
  int get socketId => _native.ref.socketId;
  @override
  int get opcode => _native.ref.opcode;
  @override
  int get flags => _native.ref.flags;

  /// Eagerly copies all fields to a plain [RawWsFrame] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  RawWsFrame toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    _releaseFn!(_native.cast());
    return v;
  }
}

// --- @HybridRecord binary extensions (generated by Nitrogen) ---

extension RawHeaderRecordFfiExt on RawHeader {
  static RawHeader fromNative(Pointer<Uint8> ptr) =>
      RawHeaderRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawTlsConfigRecordFfiExt on RawTlsConfig {
  static RawTlsConfig fromNative(Pointer<Uint8> ptr) =>
      RawTlsConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawProxyConfigRecordFfiExt on RawProxyConfig {
  static RawProxyConfig fromNative(Pointer<Uint8> ptr) =>
      RawProxyConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawDnsConfigRecordFfiExt on RawDnsConfig {
  static RawDnsConfig fromNative(Pointer<Uint8> ptr) =>
      RawDnsConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawCookieConfigRecordFfiExt on RawCookieConfig {
  static RawCookieConfig fromNative(Pointer<Uint8> ptr) =>
      RawCookieConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawPoolConfigRecordFfiExt on RawPoolConfig {
  static RawPoolConfig fromNative(Pointer<Uint8> ptr) =>
      RawPoolConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawClientConfigRecordFfiExt on RawClientConfig {
  static RawClientConfig fromNative(Pointer<Uint8> ptr) =>
      RawClientConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawRequestOptionsRecordFfiExt on RawRequestOptions {
  static RawRequestOptions fromNative(Pointer<Uint8> ptr) =>
      RawRequestOptionsRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawRequestRecordFfiExt on RawRequest {
  static RawRequest fromNative(Pointer<Uint8> ptr) =>
      RawRequestRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawTimingsRecordFfiExt on RawTimings {
  static RawTimings fromNative(Pointer<Uint8> ptr) =>
      RawTimingsRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawResponseRecordFfiExt on RawResponse {
  static RawResponse fromNative(Pointer<Uint8> ptr) =>
      RawResponseRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawResponseHeadRecordFfiExt on RawResponseHead {
  static RawResponseHead fromNative(Pointer<Uint8> ptr) =>
      RawResponseHeadRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawEventRecordFfiExt on RawEvent {
  static RawEvent fromNative(Pointer<Uint8> ptr) =>
      RawEventRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawCookieRecordFfiExt on RawCookie {
  static RawCookie fromNative(Pointer<Uint8> ptr) =>
      RawCookieRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawCacheConfigRecordFfiExt on RawCacheConfig {
  static RawCacheConfig fromNative(Pointer<Uint8> ptr) =>
      RawCacheConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawCacheStatsRecordFfiExt on RawCacheStats {
  static RawCacheStats fromNative(Pointer<Uint8> ptr) =>
      RawCacheStatsRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawWsConfigRecordFfiExt on RawWsConfig {
  static RawWsConfig fromNative(Pointer<Uint8> ptr) =>
      RawWsConfigRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension RawWsHandshakeRecordFfiExt on RawWsHandshake {
  static RawWsHandshake fromNative(Pointer<Uint8> ptr) =>
      RawWsHandshakeRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

class _NitroHttpNativeImpl extends NitroHttpNative {
  final DynamicLibrary _dylib;
  static final _instances = <String, WeakReference<_NitroHttpNativeImpl>>{};

  static final _instanceFinalizer =
      Finalizer<
        ({
          int id,
          String key,
          Pointer<NitroErrorFfi> err,
          void Function(int) destroy,
        })
      >((t) {
        try {
          t.destroy(t.id);
          NitroRuntime.releaseLib('nitro_http');
          if (_instances[t.key]?.target == null) _instances.remove(t.key);
        } catch (_) {
        } finally {
          calloc.free(t.err);
        }
      });
  final String _instanceKey;
  late final int _instanceId;
  final Pointer<NitroErrorFfi> _nitroErr = calloc<NitroErrorFfi>();
  late final void Function(Pointer<Void>) _nitroFreePtr = _dylib
      .lookupFunction<
        Void Function(Pointer<Void>),
        void Function(Pointer<Void>)
      >('nitro_http_nitro_free');
  void _nitroFree(Pointer<NativeType> ptr) => _nitroFreePtr(ptr.cast());
  late final Pointer<NativeFinalizerFunction> _nitroFreeFinalizer = _dylib
      .lookup<NativeFinalizerFunction>('nitro_http_nitro_free')
      .cast();
  late final Pointer<Void> Function(int) _nitroAllocPtr = _dylib
      .lookupFunction<
        Pointer<Void> Function(IntPtr),
        Pointer<Void> Function(int)
      >('nitro_http_nitro_alloc');
  late final NitroNativeAllocator _nitroNativeAllocator = NitroNativeAllocator(
    _nitroAllocPtr,
    _nitroFreePtr,
  );

  static DynamicLibrary _loadSupportedLibrary() {
    assert(
      !const bool.fromEnvironment('dart.library.js_interop'),
      'nitro_http: dart:ffi is unavailable on web. Instantiate the web bridge via createNitroHttpNativeWebInstance() from the generated *.web.bridge.g.dart file instead.',
    );
    return NitroRuntime.loadLibForTargets(
      'nitro_http',
      ios: true,
      android: true,
      macos: true,
      windows: true,
      linux: true,
      web: true,
    );
  }

  factory _NitroHttpNativeImpl([String key = 'default']) {
    final cached = _instances[key]?.target;
    if (cached != null) return cached;
    return _NitroHttpNativeImpl._init(key);
  }

  _NitroHttpNativeImpl._init(this._instanceKey)
    : _dylib = _loadSupportedLibrary() {
    final initSw = Stopwatch()..start();
    assert(
      sizeOf<IntPtr>() >= 4,
      'nitro_http: unsupported pointer width ${sizeOf<IntPtr>()}B',
    );
    final initFunc = _dylib
        .lookupFunction<
          IntPtr Function(Pointer<Void>),
          int Function(Pointer<Void>)
        >('nitro_http_init_dart_api_dl');
    final initCode = initFunc(NativeApi.initializeApiDLData);
    if (initCode != 0) {
      throw StateError(
        'nitro_http: Dart API DL initialization failed with code $initCode.',
      );
    }
    if (!_dylib.providesSymbol('nitro_http_nitro_abi_version')) {
      throw StateError(
        'nitro_http: missing nitro_http_nitro_abi_version — plugin must be rebuilt against the current Nitro version.',
      );
    }
    NitroRuntime.checkAbiVersion(
      'nitro_http',
      () => _dylib.lookupFunction<Uint32 Function(), int Function()>(
        'nitro_http_nitro_abi_version',
      )(),
    );
    if (!_dylib.providesSymbol('nitro_http_nitro_bridge_checksum')) {
      throw StateError(
        'nitro_http: missing nitro_http_nitro_bridge_checksum — plugin must be rebuilt against the current Nitro version.',
      );
    }
    NitroRuntime.checkLinkChecksum(
      'nitro_http',
      '8f8ae94532877555',
      () => _dylib
          .lookupFunction<Pointer<Utf8> Function(), Pointer<Utf8> Function()>(
            'nitro_http_nitro_bridge_checksum',
          )()
          .toDartString(),
    );
    RawChunkProxy._init(_dylib);
    RawWsFrameProxy._init(_dylib);
    final _keyPtr = _instanceKey.toNativeUtf8(allocator: calloc);
    try {
      _instanceId = _createInstancePtr(_keyPtr);
      if (_instanceId < 0) {
        throw StateError(
          'nitro_http: failed to create native instance for key "$_instanceKey".',
        );
      }
    } finally {
      calloc.free(_keyPtr);
    }
    NitroInstanceRegistry.register(_instanceId, this);
    _instances[_instanceKey] = WeakReference(this);
    _instanceFinalizer.attach(this, (
      id: _instanceId,
      key: _instanceKey,
      err: _nitroErr,
      destroy: _destroyInstancePtr,
    ), detach: this);
    initSw.stop();
    NitroRuntime.logLifecycle(
      'init(nitro_http)',
      'initialized in ${initSw.elapsedMicroseconds} µs (instanceId=$_instanceId)',
    );
  }

  late final int Function(Pointer<Utf8>) _createInstancePtr = _dylib
      .lookupFunction<
        Int64 Function(Pointer<Utf8>),
        int Function(Pointer<Utf8>)
      >('nitro_http_create_instance');
  late final void Function(int) _destroyInstancePtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_http_destroy_instance',
      );
  late final Pointer<Utf8> Function(int, Pointer<NitroErrorFfi>)
  _engineVersionPtr = _dylib
      .lookupFunction<
        Pointer<Utf8> Function(Int64, Pointer<NitroErrorFfi>),
        Pointer<Utf8> Function(int, Pointer<NitroErrorFfi>)
      >('nitro_http_engine_version');
  late final bool Function(int, Pointer<NitroErrorFfi>) _supportsHttp3Ptr =
      _dylib
          .lookup<NativeFunction<Bool Function(Int64, Pointer<NitroErrorFfi>)>>(
            'nitro_http_supports_http3',
          )
          .asFunction<bool Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final bool Function(int, Pointer<NitroErrorFfi>) _supportsWebSocketsPtr =
      _dylib
          .lookup<NativeFunction<Bool Function(Int64, Pointer<NitroErrorFfi>)>>(
            'nitro_http_supports_web_sockets',
          )
          .asFunction<bool Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final bool Function(int, Pointer<NitroErrorFfi>) _supportsBrotliPtr =
      _dylib
          .lookup<NativeFunction<Bool Function(Int64, Pointer<NitroErrorFfi>)>>(
            'nitro_http_supports_brotli',
          )
          .asFunction<bool Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final bool Function(int, Pointer<NitroErrorFfi>) _supportsZstdPtr =
      _dylib
          .lookup<NativeFunction<Bool Function(Int64, Pointer<NitroErrorFfi>)>>(
            'nitro_http_supports_zstd',
          )
          .asFunction<bool Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final void Function(int, Pointer<NitroErrorFfi>) _resetNativePtr = _dylib
      .lookup<NativeFunction<Void Function(Int64, Pointer<NitroErrorFfi>)>>(
        'nitro_http_reset_native',
      )
      .asFunction<void Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>)
  _configureClientPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Uint8>, Pointer<NitroErrorFfi>),
        void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>)
      >('nitro_http_configure_client');
  late final void Function(
    int,
    Pointer<Uint8>,
    Pointer<Uint8>,
    int,
    Pointer<NitroErrorFfi>,
    int,
  )
  _sendBufferedPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Size,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          Pointer<Uint8>,
          int,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_http_send_buffered');
  late final void Function(
    int,
    int,
    Pointer<Uint8>,
    Pointer<Uint8>,
    int,
    int,
    Pointer<NitroErrorFfi>,
  )
  _sendBufferedCoalescedPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Int64,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Size,
          Int64,
          Pointer<NitroErrorFfi>,
        ),
        void Function(
          int,
          int,
          Pointer<Uint8>,
          Pointer<Uint8>,
          int,
          int,
          Pointer<NitroErrorFfi>,
        )
      >('nitro_http_send_buffered_coalesced');
  late final void Function(int, int, Pointer<NitroErrorFfi>) _releaseRecordPtr =
      _dylib
          .lookup<
            NativeFunction<Void Function(Int64, Int64, Pointer<NitroErrorFfi>)>
          >('nitro_http_release_record')
          .asFunction<void Function(int, int, Pointer<NitroErrorFfi>)>(
            isLeaf: true,
          );
  late final void Function(
    int,
    Pointer<Uint8>,
    Pointer<Uint8>,
    int,
    Pointer<NitroErrorFfi>,
    int,
  )
  _startStreamedPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Size,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          Pointer<Uint8>,
          int,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_http_start_streamed');
  late final void Function(int, int, Pointer<NitroErrorFfi>) _cancelPtr = _dylib
      .lookup<
        NativeFunction<Void Function(Int64, Int64, Pointer<NitroErrorFfi>)>
      >('nitro_http_cancel')
      .asFunction<void Function(int, int, Pointer<NitroErrorFfi>)>(
        isLeaf: true,
      );
  late final void Function(int, Pointer<NitroErrorFfi>) _cancelAllPtr = _dylib
      .lookup<NativeFunction<Void Function(Int64, Pointer<NitroErrorFfi>)>>(
        'nitro_http_cancel_all',
      )
      .asFunction<void Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final void Function(int, int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
  _cancelTokenPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>),
        void Function(int, int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
      >('nitro_http_cancel_token');
  late final void Function(int, int, Pointer<NitroErrorFfi>)
  _releaseCancelTokenPtr = _dylib
      .lookup<
        NativeFunction<Void Function(Int64, Int64, Pointer<NitroErrorFfi>)>
      >('nitro_http_release_cancel_token')
      .asFunction<void Function(int, int, Pointer<NitroErrorFfi>)>(
        isLeaf: true,
      );
  late final void Function(int, int, int, int, Pointer<NitroErrorFfi>)
  _grantCreditPtr = _dylib
      .lookup<
        NativeFunction<
          Void Function(Int64, Int64, Int64, Int64, Pointer<NitroErrorFfi>)
        >
      >('nitro_http_grant_credit')
      .asFunction<void Function(int, int, int, int, Pointer<NitroErrorFfi>)>(
        isLeaf: true,
      );
  late final int Function(int, int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
  _feedUploadChunkPtr = _dylib
      .lookupFunction<
        Int64 Function(
          Int64,
          Int64,
          Pointer<Uint8>,
          Size,
          Pointer<NitroErrorFfi>,
        ),
        int Function(int, int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
      >('nitro_http_feed_upload_chunk');
  late final void Function(int, int, Pointer<NitroErrorFfi>) _finishUploadPtr =
      _dylib
          .lookup<
            NativeFunction<Void Function(Int64, Int64, Pointer<NitroErrorFfi>)>
          >('nitro_http_finish_upload')
          .asFunction<void Function(int, int, Pointer<NitroErrorFfi>)>(
            isLeaf: true,
          );
  late final void Function(int, int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
  _failUploadPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>),
        void Function(int, int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
      >('nitro_http_fail_upload');
  late final Pointer<Uint8> Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
  _getCookiesPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>),
        Pointer<Uint8> Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
      >('nitro_http_get_cookies');
  late final void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>)
  _setCookiePtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Uint8>, Pointer<NitroErrorFfi>),
        void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>)
      >('nitro_http_set_cookie');
  late final void Function(int, Pointer<NitroErrorFfi>) _clearCookiesPtr =
      _dylib
          .lookup<NativeFunction<Void Function(Int64, Pointer<NitroErrorFfi>)>>(
            'nitro_http_clear_cookies',
          )
          .asFunction<void Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final void Function(int, Pointer<NitroErrorFfi>) _flushCookiesPtr =
      _dylib
          .lookup<NativeFunction<Void Function(Int64, Pointer<NitroErrorFfi>)>>(
            'nitro_http_flush_cookies',
          )
          .asFunction<void Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>)
  _configureCachePtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Uint8>, Pointer<NitroErrorFfi>),
        void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>)
      >('nitro_http_configure_cache');
  late final void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>, int)
  _prefetchPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Uint8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>, int)
      >('nitro_http_prefetch');
  late final void Function(int, Pointer<NitroErrorFfi>) _clearCachePtr = _dylib
      .lookup<NativeFunction<Void Function(Int64, Pointer<NitroErrorFfi>)>>(
        'nitro_http_clear_cache',
      )
      .asFunction<void Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final Pointer<Uint8> Function(int, Pointer<NitroErrorFfi>)
  _cacheStatsPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64, Pointer<NitroErrorFfi>),
        Pointer<Uint8> Function(int, Pointer<NitroErrorFfi>)
      >('nitro_http_cache_stats');
  late final void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>, int)
  _wsConnectPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Uint8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>, int)
      >('nitro_http_ws_connect');
  late final int Function(int, int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
  _wsSendPtr = _dylib
      .lookupFunction<
        Int64 Function(
          Int64,
          Int64,
          Pointer<Uint8>,
          Size,
          Pointer<NitroErrorFfi>,
        ),
        int Function(int, int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
      >('nitro_http_ws_send');
  late final void Function(int, int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
  _wsClosePtr = _dylib
      .lookupFunction<
        Void Function(Int64, Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>),
        void Function(int, int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
      >('nitro_http_ws_close');
  late final void Function(int, int, int, Pointer<NitroErrorFfi>)
  _wsGrantCreditPtr = _dylib
      .lookup<
        NativeFunction<
          Void Function(Int64, Int64, Int64, Pointer<NitroErrorFfi>)
        >
      >('nitro_http_ws_grant_credit')
      .asFunction<void Function(int, int, int, Pointer<NitroErrorFfi>)>(
        isLeaf: true,
      );
  late final void Function(int, int) _registerChunksPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'nitro_http_register_chunks_stream',
      );
  late final void Function(int) _releaseChunksPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_http_release_chunks_stream',
      );
  late final void Function(int, int) _registerEventsPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'nitro_http_register_events_stream',
      );
  late final void Function(int) _releaseEventsPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_http_release_events_stream',
      );
  late final void Function(int, int) _registerWsFramesPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'nitro_http_register_ws_frames_stream',
      );
  late final void Function(int) _releaseWsFramesPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_http_release_ws_frames_stream',
      );
  // ignore: unused_field
  late final Pointer<NitroErrorFfi> Function() _getErrorPtr = _dylib
      .lookupFunction<
        Pointer<NitroErrorFfi> Function(),
        Pointer<NitroErrorFfi> Function()
      >('nitro_http_get_error');
  // ignore: unused_field
  late final void Function() _clearErrorPtr = _dylib
      .lookupFunction<Void Function(), void Function()>(
        'nitro_http_clear_error',
      );

  @override
  void dispose() {
    if (isDisposed) return;
    _instanceFinalizer.detach(this);
    NitroRuntime.logLifecycle(
      'dispose(nitro_http)',
      'disposing (instanceId=$_instanceId)',
    );
    _destroyInstancePtr(_instanceId);
    NitroRuntime.releaseLib('nitro_http');
    _instances.remove(_instanceKey);
    NitroInstanceRegistry.unregister(_instanceId, this);
    calloc.free(_nitroErr); // S8: free pre-allocated error slot
    super.dispose(); // sets isDisposed = true, calls onDestroy()
    NitroRuntime.logLifecycle('dispose(nitro_http)', 'disposed');
  }

  /// Opaque reference to this native impl — pass to other plugins via AnyNativeObject.
  AnyNativeObject get asAnyNativeObject => AnyNativeObject(_instanceId);

  @override
  String engineVersion() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _engineVersionPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      return res.toDartStringBorrowed();
    }, methodName: 'engineVersion');
  }

  @override
  bool supportsHttp3() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _supportsHttp3Ptr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      return res;
    }, methodName: 'supportsHttp3');
  }

  @override
  bool supportsWebSockets() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _supportsWebSocketsPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      return res;
    }, methodName: 'supportsWebSockets');
  }

  @override
  bool supportsBrotli() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _supportsBrotliPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      return res;
    }, methodName: 'supportsBrotli');
  }

  @override
  bool supportsZstd() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _supportsZstdPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      return res;
    }, methodName: 'supportsZstd');
  }

  @override
  void resetNative() {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _resetNativePtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'resetNative');
  }

  @override
  void configureClient(RawClientConfig config) {
    checkDisposed();
    NitroRuntime.callSync<void>(
      () => withArena((arena) {
        _configureClientPtr(_instanceId, config.toNative(arena), _nitroErr);
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      }),
      methodName: 'configureClient',
    );
  }

  @override
  Future<RawResponse> sendBuffered(RawRequest request, Uint8List body) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<RawResponse>(
        call: (port) => _sendBufferedPtr(
          _instanceId,
          request.toNative(arena),
          body.toPointer(arena),
          body.length,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "sendBuffered (native-async): native posted null for a non-nullable RawResponse result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return RawResponseRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'sendBuffered',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  void sendBufferedCoalesced(
    int callId,
    RawRequest request,
    Uint8List body,
    int dartPort,
  ) {
    checkDisposed();
    NitroRuntime.callSync<void>(
      () => withArena((arena) {
        _sendBufferedCoalescedPtr(
          _instanceId,
          callId,
          request.toNative(arena),
          body.toPointer(arena),
          body.length,
          dartPort,
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      }),
      methodName: 'sendBufferedCoalesced',
    );
  }

  @override
  void releaseRecord(int address) {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _releaseRecordPtr(_instanceId, address, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'releaseRecord');
  }

  @override
  Future<RawResponseHead> startStreamed(RawRequest request, Uint8List body) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<RawResponseHead>(
        call: (port) => _startStreamedPtr(
          _instanceId,
          request.toNative(arena),
          body.toPointer(arena),
          body.length,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "startStreamed (native-async): native posted null for a non-nullable RawResponseHead result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return RawResponseHeadRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'startStreamed',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  void cancel(int requestId) {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _cancelPtr(_instanceId, requestId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'cancel');
  }

  @override
  void cancelAll() {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _cancelAllPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'cancelAll');
  }

  @override
  void cancelToken(int tokenId, String reason) {
    checkDisposed();
    NitroRuntime.callSync<void>(
      () => withArena((arena) {
        _cancelTokenPtr(
          _instanceId,
          tokenId,
          reason.toNativeUtf8(allocator: arena),
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      }),
      methodName: 'cancelToken',
    );
  }

  @override
  void releaseCancelToken(int tokenId) {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _releaseCancelTokenPtr(_instanceId, tokenId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'releaseCancelToken');
  }

  @override
  void grantCredit(int requestId, int chunkCount, int ackedChunks) {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _grantCreditPtr(
        _instanceId,
        requestId,
        chunkCount,
        ackedChunks,
        _nitroErr,
      );
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'grantCredit');
  }

  @override
  int feedUploadChunk(int requestId, Uint8List chunk) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _feedUploadChunkPtr(
          _instanceId,
          requestId,
          chunk.toPointer(arena),
          chunk.length,
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return res;
      }),
      methodName: 'feedUploadChunk',
    );
  }

  @override
  void finishUpload(int requestId) {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _finishUploadPtr(_instanceId, requestId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'finishUpload');
  }

  @override
  void failUpload(int requestId, String message) {
    checkDisposed();
    NitroRuntime.callSync<void>(
      () => withArena((arena) {
        _failUploadPtr(
          _instanceId,
          requestId,
          message.toNativeUtf8(allocator: arena),
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      }),
      methodName: 'failUpload',
    );
  }

  @override
  List<RawCookie> getCookies(String url) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _getCookiesPtr(
          _instanceId,
          url.toNativeUtf8(allocator: arena),
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return LazyRecordList.decode(
          res,
          (r) => RawCookieRecordExt.fromReader(r),
          nativeFree: _nitroFreeFinalizer,
        );
      }),
      methodName: 'getCookies',
    );
  }

  @override
  void setCookie(RawCookie cookie) {
    checkDisposed();
    NitroRuntime.callSync<void>(
      () => withArena((arena) {
        _setCookiePtr(_instanceId, cookie.toNative(arena), _nitroErr);
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      }),
      methodName: 'setCookie',
    );
  }

  @override
  void clearCookies() {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _clearCookiesPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'clearCookies');
  }

  @override
  void flushCookies() {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _flushCookiesPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'flushCookies');
  }

  @override
  void configureCache(RawCacheConfig config) {
    checkDisposed();
    NitroRuntime.callSync<void>(
      () => withArena((arena) {
        _configureCachePtr(_instanceId, config.toNative(arena), _nitroErr);
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      }),
      methodName: 'configureCache',
    );
  }

  @override
  Future<RawResponse> prefetch(RawRequest request) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<RawResponse>(
        call: (port) =>
            _prefetchPtr(_instanceId, request.toNative(arena), _nitroErr, port),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "prefetch (native-async): native posted null for a non-nullable RawResponse result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return RawResponseRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'prefetch',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  void clearCache() {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _clearCachePtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'clearCache');
  }

  @override
  RawCacheStats cacheStats() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _cacheStatsPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      final RawCacheStats decoded;
      try {
        decoded = RawCacheStatsRecordFfiExt.fromNative(res);
      } finally {
        _nitroFree(res);
      }
      return decoded;
    }, methodName: 'cacheStats');
  }

  @override
  Future<RawWsHandshake> wsConnect(RawWsConfig config) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<RawWsHandshake>(
        call: (port) =>
            _wsConnectPtr(_instanceId, config.toNative(arena), _nitroErr, port),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "wsConnect (native-async): native posted null for a non-nullable RawWsHandshake result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return RawWsHandshakeRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'wsConnect',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  int wsSend(int opcode, Uint8List payload) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _wsSendPtr(
          _instanceId,
          opcode,
          payload.toPointer(arena),
          payload.length,
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return res;
      }),
      methodName: 'wsSend',
    );
  }

  @override
  void wsClose(int code, String reason) {
    checkDisposed();
    NitroRuntime.callSync<void>(
      () => withArena((arena) {
        _wsClosePtr(
          _instanceId,
          code,
          reason.toNativeUtf8(allocator: arena),
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      }),
      methodName: 'wsClose',
    );
  }

  @override
  void wsGrantCredit(int frameCount, int ackedFrames) {
    checkDisposed();
    NitroRuntime.callSync<void>(() {
      _wsGrantCreditPtr(_instanceId, frameCount, ackedFrames, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
    }, methodName: 'wsGrantCredit');
  }

  @override
  Stream<RawChunk> get chunks {
    checkDisposed();
    return NitroRuntime.openStream<RawChunkProxy>(
      register: (port) => _registerChunksPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError('Received null event on non-nullable stream chunks');
        }
        return RawChunkProxy(Pointer<RawChunkFfi>.fromAddress(message as int));
      },
      release: (port) => _releaseChunksPtr(port),
      backpressure: Backpressure.bufferDrop,
    );
  }

  @override
  Stream<RawEvent> get events {
    checkDisposed();
    return NitroRuntime.openStream<RawEvent>(
      register: (port) => _registerEventsPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError('Received null event on non-nullable stream events');
        }
        final rawPtr = Pointer<Uint8>.fromAddress(message as int);
        try {
          return RawEventRecordFfiExt.fromNative(rawPtr);
        } finally {
          _nitroFree(rawPtr);
        }
      },
      release: (port) => _releaseEventsPtr(port),
      backpressure: Backpressure.bufferDrop,
    );
  }

  @override
  Stream<RawWsFrame> get wsFrames {
    checkDisposed();
    return NitroRuntime.openStream<RawWsFrameProxy>(
      register: (port) => _registerWsFramesPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream wsFrames',
          );
        }
        return RawWsFrameProxy(
          Pointer<RawWsFrameFfi>.fromAddress(message as int),
        );
      },
      release: (port) => _releaseWsFramesPtr(port),
      backpressure: Backpressure.bufferDrop,
    );
  }
}

/// PX19: Creates the native (dart:ffi) implementation of [NitroHttpNative].
/// On web, import `*.web.bridge.g.dart` conditionally and call
/// `createNitroHttpNativeWebInstance()` instead.
/// A distinct [key] creates an independent native instance; the default
/// key returns the shared singleton.
NitroHttpNative nitroHttpNative_createNativeInstance([
  String key = 'default',
]) => _NitroHttpNativeImpl(key);

extension NitroHttpNativeNativeRef on NitroHttpNative {
  AnyNativeObject get asAnyNativeObject =>
      (this as _NitroHttpNativeImpl).asAnyNativeObject;
}

/// Creates the native implementation of [NitroHttpNative]. A distinct [key]
/// creates an independent native instance; the default key returns the
/// shared singleton. On web the platform shim resolves this to the WASM
/// bridge factory instead.
NitroHttpNative createNitroHttpNativeInstance([String key = 'default']) =>
    _NitroHttpNativeImpl(key);

/// No-op on native (the dynamic library loads synchronously). On web this
/// resolves to the WASM module loader and MUST be awaited before
/// [createNitroHttpNativeInstance].
Future<void> ensureNitroHttpNativeReady({String? jsUrl}) async {}
