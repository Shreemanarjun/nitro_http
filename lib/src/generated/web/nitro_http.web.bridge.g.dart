// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.4
// Generated from: nitro_http.native.dart
// ignore_for_file: non_constant_identifier_names, unnecessary_cast, unused_element, unused_field, unused_import, no_leading_underscores_for_local_identifiers, unused_local_variable
/// Web (WASM/js_interop) implementation of [NitroHttpNative]. Reached through
/// the platform shim; never compiled into a native build.
library;

import 'dart:async';
import 'dart:js_interop';

// The web-bridge barrel: always resolves to the WEB runtime (the main
// nitro.dart barrel is conditional and resolves native under the analyzer).
import 'package:nitro/web_bridge.dart';

// The spec library: abstract class + data types + pure codecs (.g.dart part).
import '../../nitro_http.native.dart';

const String _libName = 'nitro_http';
// The default asset URL is assets/packages/<package>/assets/web/<lib>.js
// (see NitroRuntime.loadWebModule).
const String _assetPackage = 'nitro_http';

NitroWasmModule get _module => NitroRuntime.webModule(_libName);

/// Loads and instantiates the nitro_http WASM module. MUST be awaited before
/// [createNitroHttpNativeInstance] on web. Idempotent. [jsUrl] overrides the
/// default asset URL.
Future<void> ensureNitroHttpNativeReady({String? jsUrl}) =>
    NitroRuntime.loadWebModule(
      _libName,
      jsUrl: jsUrl,
      assetPackage: _assetPackage,
    ).then((_) {});

/// Creates the web (WASM) implementation of [NitroHttpNative]. A distinct [key]
/// creates an independent native instance; the default key returns the
/// shared singleton. Await [ensureNitroHttpNativeReady] first.
NitroHttpNative createNitroHttpNativeInstance([String key = 'default']) =>
    _NitroHttpNativeWebImpl(key);

// ── Wire helpers (web) ──────────────────────────────────────────────────

Uint8List _nitroEncodeFramed(void Function(RecordWriter w) write) {
  final w = RecordWriter();
  write(w);
  return w.takeFramedBytes();
}

Uint8List _nitroPackStructRawChunk(
  NitroWasmModule m,
  WasmArena arena,
  RawChunk v,
) {
  final out = Uint8List(40);
  final bd = ByteData.sublistView(out);
  bd.setUint32(0, arena.copyIn(v.bytes), Endian.little);
  setInt64LE(bd, 8, v.bytes.length);
  setInt64LE(bd, 16, v.requestId);
  setInt64LE(bd, 24, v.kind);
  setInt64LE(bd, 32, v.aux);
  return out;
}

RawChunk _nitroReadStructRawChunk(NitroWasmModule m, int ptr) {
  final bd = ByteData.sublistView(m.readBytes(ptr, 40));
  final _pbytes = bd.getUint32(0, Endian.little);
  final _nbytes = getInt64LE(bd, 8);
  return RawChunk(
    bytes: (_pbytes == 0 ? Uint8List(0) : m.readBytes(_pbytes, _nbytes * 1)),
    requestId: getInt64LE(bd, 16),
    kind: getInt64LE(bd, 24),
    aux: getInt64LE(bd, 32),
  );
}

Uint8List _nitroPackStructRawWsFrame(
  NitroWasmModule m,
  WasmArena arena,
  RawWsFrame v,
) {
  final out = Uint8List(40);
  final bd = ByteData.sublistView(out);
  bd.setUint32(0, arena.copyIn(v.payload), Endian.little);
  setInt64LE(bd, 8, v.payload.length);
  setInt64LE(bd, 16, v.socketId);
  setInt64LE(bd, 24, v.opcode);
  setInt64LE(bd, 32, v.flags);
  return out;
}

RawWsFrame _nitroReadStructRawWsFrame(NitroWasmModule m, int ptr) {
  final bd = ByteData.sublistView(m.readBytes(ptr, 40));
  final _ppayload = bd.getUint32(0, Endian.little);
  final _npayload = getInt64LE(bd, 8);
  return RawWsFrame(
    payload: (_ppayload == 0
        ? Uint8List(0)
        : m.readBytes(_ppayload, _npayload * 1)),
    socketId: getInt64LE(bd, 16),
    opcode: getInt64LE(bd, 24),
    flags: getInt64LE(bd, 32),
  );
}

final class _NitroHttpNativeWebImpl extends NitroHttpNative {
  static final Map<String, _NitroHttpNativeWebImpl> _instances = {};

  factory _NitroHttpNativeWebImpl([String key = 'default']) =>
      _instances.putIfAbsent(key, () => _NitroHttpNativeWebImpl._(key));

  _NitroHttpNativeWebImpl._(this._key) {
    _m = _module;
    // One lib reference per instance, balancing the releaseLib in
    // dispose() — mirrors the FFI impl taking a loadLib per instance.
    // Without it the first dispose() dropped the count to zero and
    // evicted the module out from under every remaining instance.
    NitroRuntime.retainLib(_libName);
    NitroRuntime.checkAbiVersion(
      _libName,
      () => (_m.call(
        'nitro_http_nitro_abi_version',
        const [],
      ) as JSNumber).toDartInt,
    );
    NitroRuntime.checkLinkChecksum(
      _libName,
      '8f8ae94532877555',
      () => _m.readCString(
        dartI64(_m.call('nitro_http_nitro_bridge_checksum', const [])),
      ),
    );
    _err = WebNitroErrorSlot.alloc(_m);
    final keyArena = WasmArena(_m);
    try {
      _instanceId = dartI64(
        _m.call('nitro_http_create_instance', [keyArena.cString(_key).toJS]),
      );
    } finally {
      keyArena.releaseAll();
    }
    NitroRuntime.logLifecycle(
      'NitroHttpNative',
      'web instance created (key=$_key, id=$_instanceId)',
    );
  }

  final String _key;
  late final NitroWasmModule _m;
  late final WebNitroErrorSlot _err;
  late final int _instanceId;

  // Disposal state lives in HybridObject, NOT in a private field here.
  // A local `_disposed` SHADOWED the base state: dispose() set the copy,
  // `isDisposed` kept reading the base and stayed false forever.
  @override
  void checkDisposed() {
    if (isDisposed) {
      throw StateError(
        'NitroHttpNative (web) was disposed — create a new instance',
      );
    }
  }

  @override
  void dispose() {
    if (isDisposed) return;
    _m.call('nitro_http_destroy_instance', [jsI64(_instanceId)]);
    _err.free();
    _instances.remove(_key);
    NitroRuntime.releaseLib(_libName);
    NitroRuntime.logLifecycle(
      'NitroHttpNative',
      'web instance disposed (key=$_key)',
    );
    // Last: flips isDisposed and runs onDestroy(), matching the FFI impl.
    super.dispose();
  }

  // Legacy two-call error protocol — used by @nitroAsync methods, whose C
  // signature carries no NitroError* out-param.
  void _checkLegacyError() {
    final errPtr = dartI64(_m.call('nitro_http_get_error', const []));
    if (errPtr == 0 || _m.readU8(errPtr) == 0) return;
    final name = _m.readCString(_m.readU32(errPtr + 4));
    final message = _m.readCString(_m.readU32(errPtr + 8));
    final codePtr = _m.readU32(errPtr + 12);
    final stackPtr = _m.readU32(errPtr + 16);
    final code = codePtr != 0 ? _m.readCString(codePtr) : null;
    final stack = stackPtr != 0 ? _m.readCString(stackPtr) : null;
    _m.call('nitro_http_clear_error', const []);
    throw HybridException(
      name: name,
      message: message,
      code: code,
      stackTrace: stack,
    );
  }

  @override
  String engineVersion() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_engine_version', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return _m.readCString(dartI64(_res));
    }, methodName: 'engineVersion');
  }

  @override
  bool supportsHttp3() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_supports_http3', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return (_res as JSNumber).toDartInt != 0;
    }, methodName: 'supportsHttp3');
  }

  @override
  bool supportsWebSockets() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_supports_web_sockets', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return (_res as JSNumber).toDartInt != 0;
    }, methodName: 'supportsWebSockets');
  }

  @override
  bool supportsBrotli() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_supports_brotli', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return (_res as JSNumber).toDartInt != 0;
    }, methodName: 'supportsBrotli');
  }

  @override
  bool supportsZstd() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_supports_zstd', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return (_res as JSNumber).toDartInt != 0;
    }, methodName: 'supportsZstd');
  }

  @override
  void resetNative() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_reset_native', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'resetNative');
  }

  @override
  void configureClient(RawClientConfig config) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_configure_client', [
          jsI64(_instanceId),
          arena.copyIn(_nitroEncodeFramed((w) => config.writeFields(w))).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return;
      });
    }, methodName: 'configureClient');
  }

  @override
  Future<RawResponse> sendBuffered(RawRequest request, Uint8List body) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<RawResponse>(
      call: (dartPort) => _m.call('nitro_http_send_buffered', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => request.writeFields(w))).toJS,
        arena.copyIn(body).toJS,
        body.length.toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'sendBuffered (native-async): native posted null for a non-nullable RawResponse result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RawResponseRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'sendBuffered',
    );
  }

  @override
  void sendBufferedCoalesced(
    int callId,
    RawRequest request,
    Uint8List body,
    int dartPort,
  ) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_send_buffered_coalesced', [
          jsI64(_instanceId),
          jsI64(callId),
          arena.copyIn(_nitroEncodeFramed((w) => request.writeFields(w))).toJS,
          arena.copyIn(body).toJS,
          body.length.toJS,
          jsI64(dartPort),
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return;
      });
    }, methodName: 'sendBufferedCoalesced');
  }

  @override
  void releaseRecord(int address) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_release_record', [
        jsI64(_instanceId),
        jsI64(address),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'releaseRecord');
  }

  @override
  Future<RawResponseHead> startStreamed(RawRequest request, Uint8List body) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<RawResponseHead>(
      call: (dartPort) => _m.call('nitro_http_start_streamed', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => request.writeFields(w))).toJS,
        arena.copyIn(body).toJS,
        body.length.toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'startStreamed (native-async): native posted null for a non-nullable RawResponseHead result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RawResponseHeadRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'startStreamed',
    );
  }

  @override
  void cancel(int requestId) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_cancel', [
        jsI64(_instanceId),
        jsI64(requestId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'cancel');
  }

  @override
  void cancelAll() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_cancel_all', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'cancelAll');
  }

  @override
  void cancelToken(int tokenId, String reason) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_cancel_token', [
          jsI64(_instanceId),
          jsI64(tokenId),
          arena.cString(reason).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return;
      });
    }, methodName: 'cancelToken');
  }

  @override
  void releaseCancelToken(int tokenId) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_release_cancel_token', [
        jsI64(_instanceId),
        jsI64(tokenId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'releaseCancelToken');
  }

  @override
  void grantCredit(int requestId, int chunkCount, int ackedChunks) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_grant_credit', [
        jsI64(_instanceId),
        jsI64(requestId),
        jsI64(chunkCount),
        jsI64(ackedChunks),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'grantCredit');
  }

  @override
  int feedUploadChunk(int requestId, Uint8List chunk) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_feed_upload_chunk', [
          jsI64(_instanceId),
          jsI64(requestId),
          arena.copyIn(chunk).toJS,
          chunk.length.toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return dartI64(_res);
      });
    }, methodName: 'feedUploadChunk');
  }

  @override
  void finishUpload(int requestId) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_finish_upload', [
        jsI64(_instanceId),
        jsI64(requestId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'finishUpload');
  }

  @override
  void failUpload(int requestId, String message) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_fail_upload', [
          jsI64(_instanceId),
          jsI64(requestId),
          arena.cString(message).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return;
      });
    }, methodName: 'failUpload');
  }

  @override
  List<RawCookie> getCookies(String url) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_get_cookies', [
          jsI64(_instanceId),
          arena.cString(url).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        final _ptr = dartI64(_res);
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RecordReader.decodeIndexedListBytes(
          _framed,
          (r) => RawCookieRecordExt.fromReader(r),
        );
      });
    }, methodName: 'getCookies');
  }

  @override
  void setCookie(RawCookie cookie) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_set_cookie', [
          jsI64(_instanceId),
          arena.copyIn(_nitroEncodeFramed((w) => cookie.writeFields(w))).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return;
      });
    }, methodName: 'setCookie');
  }

  @override
  void clearCookies() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_clear_cookies', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'clearCookies');
  }

  @override
  void flushCookies() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_flush_cookies', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'flushCookies');
  }

  @override
  void configureCache(RawCacheConfig config) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_configure_cache', [
          jsI64(_instanceId),
          arena.copyIn(_nitroEncodeFramed((w) => config.writeFields(w))).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return;
      });
    }, methodName: 'configureCache');
  }

  @override
  Future<RawResponse> prefetch(RawRequest request) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<RawResponse>(
      call: (dartPort) => _m.call('nitro_http_prefetch', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => request.writeFields(w))).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'prefetch (native-async): native posted null for a non-nullable RawResponse result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RawResponseRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'prefetch',
    );
  }

  @override
  void clearCache() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_clear_cache', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'clearCache');
  }

  @override
  RawCacheStats cacheStats() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_cache_stats', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      final _ptr = dartI64(_res);
      final _framed = _m.readFramed(_ptr);
      _m.nitroFree(_ptr);
      return RawCacheStatsRecordExt.fromReader(
        RecordReader.fromFramedBytes(_framed),
      );
    }, methodName: 'cacheStats');
  }

  @override
  Future<RawWsHandshake> wsConnect(RawWsConfig config) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<RawWsHandshake>(
      call: (dartPort) => _m.call('nitro_http_ws_connect', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => config.writeFields(w))).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'wsConnect (native-async): native posted null for a non-nullable RawWsHandshake result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RawWsHandshakeRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'wsConnect',
    );
  }

  @override
  int wsSend(int opcode, Uint8List payload) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_ws_send', [
          jsI64(_instanceId),
          jsI64(opcode),
          arena.copyIn(payload).toJS,
          payload.length.toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return dartI64(_res);
      });
    }, methodName: 'wsSend');
  }

  @override
  void wsClose(int code, String reason) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_http_ws_close', [
          jsI64(_instanceId),
          jsI64(code),
          arena.cString(reason).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return;
      });
    }, methodName: 'wsClose');
  }

  @override
  void wsGrantCredit(int frameCount, int ackedFrames) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_http_ws_grant_credit', [
        jsI64(_instanceId),
        jsI64(frameCount),
        jsI64(ackedFrames),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return;
    }, methodName: 'wsGrantCredit');
  }

  @override
  Stream<RawChunk> get chunks {
    checkDisposed();
    return NitroRuntime.openStream<RawChunk>(
      register: (port) => _m.call('nitro_http_register_chunks_stream', [
        jsI64(_instanceId),
        jsI64(port),
      ]),
      unpack: (message) {
        if (message == null) {
          throw StateError('Received null event on non-nullable stream chunks');
        }
        final _ptr = (message as num).toInt();
        final _v = _nitroReadStructRawChunk(_m, _ptr);
        _m.nitroFree(_ptr);
        return _v;
      },
      release: (port) =>
          _m.call('nitro_http_release_chunks_stream', [jsI64(port)]),
      backpressure: Backpressure.bufferDrop,
      debugLabel: 'chunks',
    );
  }

  @override
  Stream<RawEvent> get events {
    checkDisposed();
    return NitroRuntime.openStream<RawEvent>(
      register: (port) => _m.call('nitro_http_register_events_stream', [
        jsI64(_instanceId),
        jsI64(port),
      ]),
      unpack: (message) {
        if (message == null) {
          throw StateError('Received null event on non-nullable stream events');
        }
        final _ptr = (message as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RawEventRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      release: (port) =>
          _m.call('nitro_http_release_events_stream', [jsI64(port)]),
      backpressure: Backpressure.bufferDrop,
      debugLabel: 'events',
    );
  }

  @override
  Stream<RawWsFrame> get wsFrames {
    checkDisposed();
    return NitroRuntime.openStream<RawWsFrame>(
      register: (port) => _m.call('nitro_http_register_ws_frames_stream', [
        jsI64(_instanceId),
        jsI64(port),
      ]),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream wsFrames',
          );
        }
        final _ptr = (message as num).toInt();
        final _v = _nitroReadStructRawWsFrame(_m, _ptr);
        _m.nitroFree(_ptr);
        return _v;
      },
      release: (port) =>
          _m.call('nitro_http_release_ws_frames_stream', [jsI64(port)]),
      backpressure: Backpressure.bufferDrop,
      debugLabel: 'wsFrames',
    );
  }
}
